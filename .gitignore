# Genius pipeline internals (raw traces, logs, generated-test scratch, venvs)
.genius/
**/.genius/

# Node / Next.js (per-project .gitignore also applies)
node_modules/
.next/

# SQLite sidecars & OS noise
*.db-wal
*.db-shm
.DS_Store

# pipeline state & archives (local only)
*.bak
job.json
.agents/CURRENT_PROG.md

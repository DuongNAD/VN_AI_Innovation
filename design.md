```json
{
 "project_name": "ai-public-service-assistant",
 "description": "GOAL: 'Trợ lý Thủ tục Hành chính' — an AI-guided Vietnamese public-service assistant (hackathon build, pilotable at a district office). Citizens describe an intent in free Vietnamese text or voice, are guided through DB-driven clarifying questions, receive a grounded document checklist with per-item legal reasons, fill a versioned dynamic form (prefilled from their answers), get a deterministic pre-submission validation report with an AI explanation, and administrators approve regulatory form-version changes that take effect immediately — including a two-phase, user-confirmed data migration for in-flight applications. Stack: Next.js 14 App Router (Node runtime only), TypeScript strict, Prisma + PostgreSQL, Tailwind, Vitest. AI-NATIVE ARCHITECTURE: the core is a grounded three-stage pipeline, not a chatbot on CRUD. Stage 1 intent: a deterministic diacritic-folded Vietnamese keyword matcher runs first; only on miss does the LLM classify — and it may only choose a procedureCode from the DB catalog passed in the prompt, returning JSON {procedureCode|null, confidence}; confidence < 0.6 yields an honest 'not recognized' answer. Stage 2 intake and guidance: a database-driven question state machine (no free LLM dialogue); every checklist item, step, fee and reason is retrieved from versioned regulation rows — structured-data grounding instead of generative text. Stage 3 validation: a pure rule engine produces error codes from DB rules; the LLM sees ONLY {code, field ids} (never values, never legal text) and explains them in plain Vietnamese. STT/TTS gateways add voice accessibility for elderly users. A plain-LLM build would hallucinate legal requirements and a non-AI build cannot map unaccented free-text/voice Vietnamese intents nor explain errors in plain language; this split delivers both. Every AI seam has a deterministic mock provider (AI_PROVIDER=mock) and live-failure fallback to mock with degraded=true — no upstream failure ever 5xxes. UX FLOW: home → /chat (intent search; procedure card shows name, confidence %, official source link; user confirms — a correction moment) → clarifying questions with progress 'Câu x/y' and 'Sửa câu trả lời' one-step correction (downstream answers pruned and disclosed) → /checklist (conditional documents tagged with reasons; source/version/lastChecked footer; disclaimer; listen button) → /form/{id} pinned to a form version (regulation-update banner → diff preview → user-resolved field mapping, nothing preselected) → /result report separating deterministic errors from the labeled AI explanation → /admin approval flips the active version live; /widget-demo embeds the whole flow. Trust cues: confidence, sources, versions, eco/degraded badge when mock or fallback is active, exact disclaimer on every legal screen. SAFETY & GROUNDING: all legal content flows through IProcedureDataProvider and is structurally validated by schema-guards (DATA_INTEGRITY fail-closed); LLM constrained to catalog selection + code explanation; https-only source links; the DISCLAIMER constant discloses AI limits verbatim. SECURITY DECISIONS (this revision): (1) session capability tokens are 256-bit CSPRNG values returned exactly once, stored ONLY as SHA-256 hashes, verified with constant-time digest comparison, expiring after 24h; (2) idempotency is database-backed and exactly-once — the unique-keyed IdempotencyRecord is inserted in the SAME Prisma transaction as the mutation, so a crash rolls back both and a retry re-executes cleanly, while a committed duplicate replays the stored response; (3) approval is serialized PER FORM via a Form.revision compare-and-swap inside the transaction after claiming the change request, versions are re-read inside the transaction, the target must be a DRAFT strictly newer than the current active, and a one-ACTIVE invariant is asserted before commit; (4) application saves and migration commits use a revision compare-and-swap (409 CONCURRENT_UPDATE), and migration recomputes from data re-read inside its transaction; (5) TRUST_PROXY defaults OFF (all clients share one fail-closed bucket); '1' is an explicit deployment opt-in that trusts the LAST x-forwarded-for entry appended by the platform edge; (6) TTS cache keys hash JSON.stringify([text,voice,speed,language]) (unambiguous) and voice/speed/language are strictly allowlisted; (7) audio uploads are size-capped via Content-Length BEFORE multipart parsing (413), re-checked after, and parsed with the hardened music-metadata library which enforces container allowlist and a 60s duration cap for ALL formats; (8) regex rules must pass safe-regex2 (star-height ≤ 1) plus length caps at seed/approval parse time, re-checked at runtime (RULE_CONFIG_INVALID warning) — ReDoS-safe; (9) OPENAI_BASE_URL is validated at startup (https required in production; localhost http allowed in dev; no embedded credentials) and every upstream response is bounded (status, content-type, max bytes, JSON shape) before use; (10) shared X-Admin-Token is a documented DEMO-ONLY mechanism: production startup fails unless the token is ≥ 24 chars and not the dev default; comparison is constant-time; failed attempts are limited (5/15min/IP); reviewedBy records 'shared-admin-token'; (11) invalid presetAnswers are REJECTED with 400 (never silently dropped); (12) transcripts are returned, never persisted. CONVENTIONS (stated once, apply to every API file): all v1 routes wrap handlers in handleRoute (AppError → {error:{code,message,details?}} with status, details.retryAfterSeconds → Retry-After header; unknown errors → 500 INTERNAL_ERROR generic Vietnamese message, logged server-side without leaking internals); every public route calls enforceRateLimit(routeBucket, req) first; session routes authenticate header X-Session-Token against Session.accessTokenHash; AI-touching responses spread aiMeta = {aiMode: provider name, degraded}; session load order: 404 SESSION_NOT_FOUND → 404 SESSION_EXPIRED → 403 token check; all user-facing strings Vietnamese, all code/identifiers English; pages/components never query regulation content with raw Prisma — only via authorized APIs or the provider (documented exceptions: transactional app data in routes, sources page + admin overview via provider.getCatalogOverview). The v1 surface is exactly 15 endpoints. TEST STRATEGY: DB-free unit suites (rule-engine, form-versioning, migration, intake-machine, infra) via 'npm run test:unit'; tests/integration/flow.test.ts exercises real route handlers against DATABASE_URL and THROWS in beforeAll when it is unset (explicit opt-out only via SKIP_INTEGRATION=1) so CI cannot green-wash. TRACEABILITY (guarantee → required test): token hashing/constant-time/expiry → integration §2 + infra auth; transcript non-persistence → integration §7; TTS key unambiguity + allowlists → infra tts + integration §8; proxy-trust default-off/opt-in → infra rate-limit + integration §9; upstream bounds + base-URL policy → infra upstream/config (stubbed fetch); ReDoS rejection → infra schema-guards + rule-engine adversarial; multi-CR same-form race + target-newer rule → integration §5; PUT/migrate optimistic concurrency → integration §4/§6; idempotency exactly-once + crash recovery → integration §2/§10; validate version-access control → integration §4; audio pre-parse cap + all-format duration cap → integration §7; canonical 3-error contract → rule-engine test (stated there ONCE, referenced elsewhere). DONE-WHEN: with DATABASE_URL set and AI_PROVIDER=mock — 'npx prisma db push && npx tsx prisma/seed.ts && npm test' passes (unit + full integration flow: keyword search ≥0.9 → intake with replayed idempotent answer → checklist with conditional DIVORCE_DOCUMENT + exact disclaimer → prefilled v1.0 application → canonical 3-error report with mock AI explanation → admin approve activates 2.0 immediately → two-phase migration with user-resolved residence mapping → speech gateways enforce caps and cache); 'npm run build' and 'docker build .' succeed; render.yaml deploys to a public URL. ASSUMPTIONS (architect-added, not user requirements): single-instance in-memory cache and rate limiter (Redis out of scope); Render + managed Postgres is the deployment target and its edge proxy justifies TRUST_PROXY=1 there; shared-token admin is demo-only with the production strength guard standing in for real IAM; music-metadata and safe-regex2 are the two hardening dependencies accepted beyond the base stack; custom OpenAI-compatible base URLs are allowed by config policy (scheme-validated, not host-allowlisted); IdempotencyRecord rows are retained without TTL; session revocation beyond 24h expiry is out of scope; mock fallback after live AI failure is preferred over surfacing upstream errors.",
 "files": [
  {
   "path": "package.json",
   "specification": "PURPOSE: project manifest. HOW: name ai-public-service-assistant, private, type module. scripts: dev 'next dev'; build 'prisma generate && next build'; start 'next start'; db:push 'prisma db push'; db:seed 'tsx prisma/seed.ts'; test 'vitest run'; test:unit 'vitest run --exclude tests/integration/**'; postinstall 'prisma generate'. prisma.seed = 'tsx prisma/seed.ts'. dependencies: next ^14, react ^18, react-dom ^18, @prisma/client ^5, music-metadata ^10, safe-regex2 ^4, tsx ^4. devDependencies: prisma ^5, typescript ^5, vitest ^1, @types/react, @types/react-dom, @types/node, tailwindcss ^3, postcss, autoprefixer. HOW TO TEST: npm install && npm run build succeed."
  },
  {
   "path": "tsconfig.json",
   "specification": "PURPOSE: TypeScript config. HOW: strict true, target ES2022, module esnext, moduleResolution bundler, jsx preserve, noEmit, esModuleInterop, resolveJsonModule, isolatedModules, plugin next; paths {'@/*': ['./src/*']}; include next-env.d.ts, src, tests, prisma, .next/types; exclude node_modules. HOW TO TEST: tsc --noEmit clean via next build."
  },
  {
   "path": "next.config.mjs",
   "specification": "PURPOSE: Next config. HOW: reactStrictMode true; experimental.instrumentationHook true (enables src/instrumentation.ts startup validation). Nothing else. HOW TO TEST: build succeeds; startup logs from instrumentation appear on next start.\n\nREVISION (reviewer feedback — MUST implement): Add experimental.serverComponentsExternalPackages: ['music-metadata'] — music-metadata v10 is pure ESM and next build may fail to bundle it into server components without this escape hatch."
  },
  {
   "path": "tailwind.config.ts",
   "specification": "PURPOSE: Tailwind setup. HOW: content ['./src/**/*.{ts,tsx}']; default theme; no plugins. HOW TO TEST: classes in components produce styles."
  },
  {
   "path": "postcss.config.mjs",
   "specification": "PURPOSE: PostCSS for Tailwind. HOW: plugins tailwindcss + autoprefixer. HOW TO TEST: build succeeds."
  },
  {
   "path": "vitest.config.ts",
   "specification": "PURPOSE: test runner config. HOW: defineConfig with test.environment 'node', include ['tests/**/*.test.ts'], alias '@' -> './src', testTimeout 30000, pool 'forks', fileParallelism false (integration mutates one shared database and process.env; unit files also toggle env — serial files avoid bleed). HOW TO TEST: npm run test:unit runs the five unit files; npm test additionally runs tests/integration/flow.test.ts."
  },
  {
   "path": ".env.example",
   "specification": "PURPOSE: document configuration with PLACEHOLDER values only (never real secrets). CONTENT: commented entries — DATABASE_URL=postgresql://user:pass@localhost:5432/psp; AI_PROVIDER=mock (mock|openai); OPENAI_API_KEY= (required only when AI_PROVIDER=openai); OPENAI_BASE_URL=https://api.openai.com/v1 (https required in production; http only for localhost in dev); LLM_MODEL=gpt-4o-mini; STT_MODEL=whisper-1; TTS_MODEL=tts-1; ADMIN_TOKEN=demo-admin-token (DEMO ONLY — production refuses to start unless ≥ 24 chars and not this default); RATE_LIMIT_PER_MINUTE=10; TRUST_PROXY=0 (set 1 ONLY behind a trusted edge proxy that appends x-forwarded-for, e.g. Render); SESSION_TTL_HOURS=24. HOW TO TEST: reviewed by eye; app runs with only DATABASE_URL set."
  },
  {
   "path": "prisma/schema.prisma",
   "specification": "PURPOSE: data model. HOW: datasource postgresql (env DATABASE_URL), generator client. Models: Procedure {id cuid, code String @unique, name, sector, agency, sourceUrl, lastCheckedAt DateTime, relations}. ProcedureVersion {id, procedureId FK, version String, status String, effectiveFrom DateTime?, effectiveTo DateTime?, stepsJson Json (array of {order,title,description}), durationText String, feesText String, @@unique([procedureId, version])}. ClarifyingQuestion {id, procedureId FK, code, orderNumber Int, fieldType String (radio|select|text|province), optionsJson Json?, conditionJson Json?, questionText, @@unique([procedureId, code])}. DocumentRequirement {id, procedureId FK, code, name, originals Int, copies Int, orderNumber Int, conditionJson Json?, reasonText String?}. Form {id, code @unique, procedureId FK, name, revision Int @default(0) — the per-form approval serialization counter}. FormVersion {id, formId FK, version, status String (DRAFT|ACTIVE|RETIRED), effectiveFrom DateTime?, effectiveTo DateTime?, schemaJson Json ({fields:[...], migrationHints:[...]}), @@unique([formId, version])}. ValidationRule {id, formVersionId FK, type String, fieldId String?, paramsJson Json, message String, suggestion String, severity String @default('error'), orderNumber Int}. Session {id cuid, accessTokenHash String (NEVER the plaintext token), procedureCode, intent String?, answersJson Json, currentStep Int @default(0), expiresAt DateTime, createdAt, updatedAt @updatedAt}. Application {id cuid, sessionId FK, formVersionId FK, status String @default('DRAFT'), dataJson Json, revision Int @default(0), createdAt, updatedAt @updatedAt}. ChangeRequest {id cuid, oldVersionId FK -> FormVersion, status String @default('PENDING'), sourceUrl, diffJson Json, proposedDataJson Json ({targetVersion}), reviewedBy String?, reviewedAt DateTime?, createdAt}. AiUsageLog {id, serviceType String (llm|stt|tts), model, promptTokens Int?, completionTokens Int?, audioSeconds Float?, latencyMs Int, cacheHit Boolean @default(false), degraded Boolean @default(false), createdAt — deliberately NO text/transcript column (transcript non-persistence is schema-enforced)}. IdempotencyRecord {key String @id, httpStatus Int, responseJson Json, createdAt}. HOW TO TEST: prisma db push clean; integration asserts Session stores a hash and AiUsageLog rows carry only numeric/flag fields.\n\nREVISION (reviewer feedback — MUST implement): Two additions: (1) ProcedureVersion gains legalBasisText String? — the legal-basis line rendered on the checklist and /sources (e.g. 'Luật Hộ tịch 2014; NĐ 123/2015/NĐ-CP; TT 04/2020/TT-BTP; NĐ 104/2022/NĐ-CP'). (2) DocumentRequirement gains submissionType String @default(\"SUBMIT\") with values 'SUBMIT' (nộp bản giấy/điện tử), 'PRESENT' (xuất trình để đối chiếu, không nộp), 'SYSTEM_LOOKUP' (cơ quan tự tra cứu CSDL quốc gia về dân cư — citizen submits nothing). Steps stored in stepsJson gain an 'example' string field (one concrete illustrative sentence per step)."
  },
  {
   "path": "prisma/seed.ts",
   "specification": "PURPOSE: idempotent seed of the two demo procedures, marriage form v1.0 (ACTIVE) + v2.0 (DRAFT), rules, questions, documents, and one PENDING change request. SIGNATURES: export async function main(); executed when run directly. HOW: every schemaJson/rules payload is passed through schema-guards parse functions BEFORE writing (seed fails loudly on integrity bugs); all writes are upserts keyed by unique codes; nothing is deleted, so running twice changes no row counts. DATA: Procedure MARRIAGE_REGISTRATION {name 'Đăng ký kết hôn', sector 'Hộ tịch', agency 'UBND cấp xã', sourceUrl 'https://dichvucong.gov.vn/p/home/dvc-chi-tiet-thu-tuc-hanh-chinh.html?ma_thu_tuc=1.000894', lastCheckedAt now} + ProcedureVersion 1.0 ACTIVE effectiveFrom 2026-01-01T00:00:00+07:00 with 7 stepsJson entries (chuẩn bị giấy tờ → nhận kết quả), durationText 'Trong ngày làm việc nếu hồ sơ hợp lệ', feesText 'Miễn phí (đăng ký trong nước)'. Questions (order): has_foreign_element radio boolean 1; previously_married radio boolean 2; province province 3; submission_channel radio [online, offline] 4. Documents: CCCD_BOTH (2 originals), SINGLE_STATUS_CERT, RESIDENCE_PROOF unconditional order 1-3; DIVORCE_DOCUMENT order 4 conditionJson {field previously_married, operator equals, value true} reasonText EXACTLY 'Áp dụng vì bạn đã từng đăng ký kết hôn'. Form MARRIAGE_REGISTRATION: v1.0 ACTIVE (effectiveFrom 2026-01-01+07) fields: male_full_name text, male_birth_date date, male_identity_number text, female_full_name text, female_birth_date date, female_identity_number text, residence text, province province, previously_married radio boolean, marriage_number number visibleWhen {previously_married equals true}, divorce_document file visibleWhen {previously_married equals true}, submission_channel radio [online, offline] — all labels Vietnamese. Rules v1.0 (orderNumber ascending): required on the 8 always-visible fields + submission_channel; regex ^[0-9]{12}$ on both identity numbers (message 'Số CCCD phải gồm đúng 12 chữ số', suggestion 'Kiểm tra lại số CCCD trên thẻ căn cước'); date_not_future both birth dates; number_range marriage_number {min 1, max 10}; cross_field_conflict {fields [previously_married, marriage_number], when previously_married=false AND marriage_number not empty} message 'Thông tin mâu thuẫn: chưa từng kết hôn nhưng có số lần kết hôn' suggestion vi; conditional_document divorce_document when previously_married=true. v2.0 DRAFT (no effectiveFrom): drops residence; adds permanent_address text, temporary_address text, phone_number text with regex ^0[0-9]{9}$; migrationHints [{from 'residence', candidates ['permanent_address','temporary_address']}]; rules mirrored for the new fields. ChangeRequest PENDING {oldVersionId = v1.0, sourceUrl as above, diffJson {summary 'Cập nhật biểu mẫu theo quy định mới về cư trú', added [permanent_address, temporary_address, phone_number with labels], removed [residence], changed []}, proposedDataJson {targetVersion '2.0'}}. Procedure BIRTH_REGISTRATION analogous, minimal: 1 form v1.0 ACTIVE (child_name, birth_date, province, ...), 3 questions, 3 documents. EDGES: exits non-zero on any guard failure. HOW TO TEST: integration setup runs main() twice and asserts stable counts and marriage versions exactly {1.0, 2.0}.\n\nREVISION (reviewer feedback — MUST implement): (a) Every stepsJson step gets an 'example' field — one short concrete Vietnamese sentence illustrating that step (per the brief's 'examples to illustrate each step'). (b) Every ProcedureVersion gets legalBasisText: marriage/birth -> 'Luật Hộ tịch 2014; Nghị định 123/2015/NĐ-CP; Thông tư 04/2020/TT-BTP' (marriage additionally '; Nghị định 104/2022/NĐ-CP'). (c) CORRECT the marriage checklist to match procedure 1.000894 as amended by NĐ 104/2022/NĐ-CP: Tờ khai đăng ký kết hôn = SUBMIT; Giấy xác nhận tình trạng hôn nhân = SUBMIT (when required); CCCD/hộ chiếu của hai bên = PRESENT (xuất trình, KHÔNG phải nộp); giấy tờ chứng minh cư trú = SYSTEM_LOOKUP with description noting the agency queries the CSDL quốc gia về dân cư since 01/01/2023 (citizen submits nothing); trích lục ly hôn (conditional previously_married=true) = SUBMIT. (d) Seed THREE additional guidance-only procedures (procedure + version + clarifying questions + document checklist with submissionType + steps-with-examples + fees/duration/agency + legalBasisText + dichvucong.gov.vn source_url; NO Form/FormVersion rows): TEMP_RESIDENCE_REGISTRATION (đăng ký tạm trú — Luật Cư trú 2020), CITIZEN_ID_ISSUANCE (cấp thẻ căn cước — Luật Căn cước 2023), PASSPORT_ISSUANCE (cấp hộ chiếu phổ thông trong nước — Luật Xuất cảnh, nhập cảnh 2019). Use the official dichvucong.gov.vn detail-page URL pattern; where the exact ma_thu_tuc is uncertain link the TTHC search page instead of inventing an id. Keep every string short — the seed must stay readable and the transaction fast. Intent keyword table gains synonyms for the three new procedures (tạm trú, căn cước/CCCD, hộ chiếu/passport)."
  },
  {
   "path": "openapi.yaml",
   "specification": "PURPOSE: the API contract served at /api/openapi. HOW: OpenAPI 3.0; info in English; servers ['/api/v1']; securitySchemes SessionToken (apiKey header X-Session-Token) and AdminToken (apiKey header X-Admin-Token); components.schemas ErrorEnvelope {error:{code,message,details?}} plus request/response bodies matching each route file. PATHS (exactly the 15 v1 endpoints): POST /procedures/search; POST /guided-intake/start; POST /guided-intake/{sessionId}/answer; GET /guided-intake/{sessionId}/guidance; GET /forms/{formCode}/active; POST /forms/{formCode}/validate; POST /applications; GET /applications/{id}; PUT /applications/{id}; POST /applications/{id}/migrate; POST /speech/transcribe; POST /speech/synthesize; GET /admin/change-requests; POST /admin/change-requests/{id}/approve; GET /admin/overview. Document error codes per route (RATE_LIMITED 429 with Retry-After, VERSION_NOT_ACCESSIBLE 403, CONCURRENT_UPDATE 409, etc.). HOW TO TEST: served verbatim by src/app/api/openapi/route.ts; kept in sync manually (stated limitation)."
  },
  {
   "path": "Dockerfile",
   "specification": "PURPOSE: single deployable image. HOW: FROM node:20-alpine; WORKDIR /app; COPY package*.json prisma ./ then npm ci (postinstall runs prisma generate); COPY . .; RUN npm run build; ENV NODE_ENV=production PORT=3000; EXPOSE 3000; CMD ['sh','-c','npx prisma db push --skip-generate && npx tsx prisma/seed.ts && npm run start'] — db push + idempotent seed at boot keep the demo self-provisioning against the attached Postgres. EDGES: build fails fast if prisma schema invalid. HOW TO TEST: docker build . succeeds; container with DATABASE_URL + ADMIN_TOKEN serves the app."
  },
  {
   "path": "render.yaml",
   "specification": "PURPOSE: the ONE platform config — Render blueprint for a public URL. HOW: databases: [{name: psp-db, plan: free}]; services: [{type: web, name: psp-assistant, runtime: docker, plan: free, healthCheckPath: /, envVars: DATABASE_URL fromDatabase {name psp-db, property connectionString}; ADMIN_TOKEN {generateValue: true} (satisfies the production strength guard with platform-generated randomness); AI_PROVIDER 'mock'; TRUST_PROXY '1' (explicit opt-in: Render's edge terminates TLS and appends the client IP as the LAST x-forwarded-for entry, matching src/lib/http.ts); SESSION_TTL_HOURS '24'; NODE_ENV 'production'}]. OPENAI_* left unset (mock mode). HOW TO TEST: render blueprint deploy reaches a live URL running the full flow in mock mode.\n\nREVISION (reviewer feedback — MUST implement): Ship the judged deployment with the REAL provider: AI_PROVIDER=openai and OPENAI_API_KEY declared as an env var with sync: false (set in the dashboard, never committed). The mock provider remains the automatic runtime fallback when the upstream errors, so demo safety is unchanged. Also set RATE_LIMIT_PER_MINUTE: '120' — the venue Wi-Fi NATs every judge behind one public IP, and the default 10/min shared bucket would 429 mid-demo. Keep TRUST_PROXY=1 and the separate admin-auth limiter as is."
  },
  {
   "path": "src/instrumentation.ts",
   "specification": "PURPOSE: startup configuration validation (runs once per server boot via Next instrumentation hook). SIGNATURES: export async function register(): Promise<void>. HOW: if process.env.NEXT_RUNTIME === 'nodejs', import assertStartupConfig from '@/lib/config' and call it; let it throw — a misconfigured production process must not start. Log one line stating aiMode and trustProxy (never any secret value). HOW TO TEST: infra config cases test assertStartupConfig directly; manual: production start with ADMIN_TOKEN 'demo-admin-token' aborts."
  },
  {
   "path": "src/lib/constants.ts",
   "specification": "PURPOSE: single source for shared literal contracts (importable from server and client code; no hooks, no node APIs). SIGNATURES: export const DISCLAIMER: string = EXACTLY 'Thông tin chỉ mang tính hướng dẫn, không thay thế văn bản pháp luật hiện hành. Vui lòng đối chiếu với nguồn chính thức trước khi nộp hồ sơ.'; export const PROVINCES: readonly string[] — the 34 post-2025-merger units in this order: Hà Nội, TP. Hồ Chí Minh, Hải Phòng, Đà Nẵng, Cần Thơ, Huế, Lai Châu, Điện Biên, Sơn La, Lạng Sơn, Cao Bằng, Tuyên Quang, Lào Cai, Thái Nguyên, Phú Thọ, Bắc Ninh, Hưng Yên, Ninh Bình, Quảng Ninh, Thanh Hóa, Nghệ An, Hà Tĩnh, Quảng Trị, Quảng Ngãi, Gia Lai, Khánh Hòa, Lâm Đồng, Đắk Lắk, Đồng Nai, Tây Ninh, Vĩnh Long, Đồng Tháp, An Giang, Cà Mau; export const LIMITS = {JSON_BODY_MAX_BYTES: 100000, AUDIO_MAX_BYTES: 5242880, AUDIO_PREPARSE_MAX_BYTES: 6291456, AUDIO_MAX_SECONDS: 60, TTS_TEXT_MAX: 500, ANSWER_TEXT_MAX: 500, FIELD_VALUE_MAX: 2000} as const. HOW TO TEST: intake-machine test locks DISCLAIMER by strict equality; PROVINCES length 34 asserted in infra sanity case."
  },
  {
   "path": "src/lib/errors.ts",
   "specification": "PURPOSE: error type, envelope, and the route wrapper used by every API file. SIGNATURES: export class AppError extends Error {readonly status: number; readonly code: string; readonly details?: unknown; constructor(status, code, message, details?)}; export function jsonOk(body: unknown, init?: {status?: number; headers?: Record<string,string>}): Response; export function handleRoute<C>(fn: (req: Request, ctx: C) => Promise<Response>): (req: Request, ctx: C) => Promise<Response>. HOW: handleRoute try/catches: AppError → Response JSON {error: {code, message, details}} with err.status; when details is an object containing numeric retryAfterSeconds, also set header Retry-After; any other throw → console.error the real error server-side, respond 500 {error: {code 'INTERNAL_ERROR', message 'Đã xảy ra lỗi hệ thống. Vui lòng thử lại sau.'}} with NO internal detail. EDGES: details must be JSON-serializable; never include stack traces or token values in envelopes. HOW TO TEST: exercised by every route test; infra asserts a thrown AppError(429,...,{retryAfterSeconds}) surfaces the Retry-After header."
  },
  {
   "path": "src/lib/config.ts",
   "specification": "PURPOSE: validated environment access — the only module reading process.env (secrets are consumed, never echoed). SIGNATURES: export function isProd(): boolean; export function getTrustProxy(): boolean (TRUST_PROXY === '1'; DEFAULT FALSE); export function getRateLimitPerMinute(): number (parseInt; NaN or < 1 → 10); export function getSessionTtlHours(): number (default 24); export function getAiProvider(): 'mock'|'openai' (unknown value → 'mock'); export function getOpenAiKey(): string (throws CONFIG_INVALID when openai mode and unset); export function getOpenAiBaseUrl(): string; export function getAdminToken(): string; export function assertStartupConfig(): void. HOW: getters read process.env on EVERY call (no memoization — tests vary env). getOpenAiBaseUrl: default 'https://api.openai.com/v1'; when set, must parse with new URL; protocol must be https:, OR http: only when hostname is localhost/127.0.0.1 AND !isProd(); reject embedded username/password; strip one trailing slash; violations throw Error('CONFIG_INVALID: ...') naming the rule but never echoing secrets. getAdminToken: value of ADMIN_TOKEN or dev fallback 'demo-admin-token'; in production throws unless set, length ≥ 24, and !== 'demo-admin-token' (the demo-only shared-token restriction, enforced at startup per the stated assumption). assertStartupConfig: calls getAdminToken, getOpenAiBaseUrl, and (openai mode) getOpenAiKey. EDGES: never log values. HOW TO TEST: infra config section — prod+weak token throws; prod+http base URL throws; dev+http://localhost passes; credentialed URL throws; RATE_LIMIT_PER_MINUTE 'abc' and '-5' → 10."
  },
  {
   "path": "src/lib/http.ts",
   "specification": "PURPOSE: request parsing helpers, client identity, and aiMeta. SIGNATURES: export async function readJsonBody(req: Request, maxBytes = LIMITS.JSON_BODY_MAX_BYTES): Promise<Record<string, unknown>>; export function requireString(body, key, maxLen = 1000): string; export function optionalString(body, key, maxLen = 1000): string | undefined; export function optionalObject(body, key): Record<string, unknown> | undefined; export function getClientIp(req: Request): string; export function aiMeta(provider: string, degraded: boolean): {aiMode: string; degraded: boolean}. HOW: readJsonBody — reject Content-Length > maxBytes early (400 BODY_TOO_LARGE); await req.text(), re-check byte length; JSON.parse in try/catch → 400 INVALID_JSON; result must be a plain object (not array/null) → 400 INVALID_JSON. requireString — missing/non-string/empty-after-trim → 400 INVALID_INPUT with details {field}; over maxLen → 400 VALUE_TOO_LONG. getClientIp — getTrustProxy() false → return the constant 'global' (every client shares one fail-closed bucket; forged headers are IGNORED entirely); true → take x-forwarded-for, split on comma, return the LAST trimmed entry (appended by the trusted edge; client-prepended spoof entries are thereby ignored); absent header → 'global'. EDGES: never trust x-real-ip. HOW TO TEST: infra rate-limit section covers both trust modes; INVALID_JSON and BODY_TOO_LARGE covered in integration §2."
  },
  {
   "path": "src/lib/auth.ts",
   "specification": "PURPOSE: hashing, capability tokens, session auth, admin auth — the token-security contract. SIGNATURES: export function sha256hex(input: string | Buffer): string; export function generateAccessToken(): string; export function hashToken(token: string): string; export function verifyToken(token: string, storedHash: string): boolean; export function requireSessionToken(req: Request, storedHash: string): void; export function requireAdmin(req: Request): void. HOW: generateAccessToken = crypto.randomBytes(32).toString('base64url') (256-bit CSPRNG, returned to the client exactly once by the start route, never logged anywhere). hashToken = sha256hex('psp-session:' + token). verifyToken: compute hashToken(token), compare with crypto.timingSafeEqual over the two utf8 hex buffers — both are always 64 bytes so length is constant and comparison is constant-time; any exception → false. requireSessionToken: header X-Session-Token; missing, empty, longer than 200 chars, or verifyToken false → AppError(403, 'FORBIDDEN', 'Không có quyền truy cập phiên này.'). requireAdmin: presented = header X-Admin-Token ?? ''; first call rateLimitCheck('admin-auth', req, 5, 900000) (throws 429 RATE_LIMITED when the failure budget is spent); compare sha256 digests of presented vs getAdminToken() via timingSafeEqual; on mismatch call rateLimitConsume('admin-auth', req) then throw AppError(401, 'UNAUTHORIZED', 'Sai mã quản trị.'); on match return without consuming (only FAILURES count). EDGES: no token value ever appears in errors or logs. HOW TO TEST: infra auth section — token is 43-char base64url, two generations differ; verifyToken true/false/malformed; requireSessionToken 403 matrix. Integration §2 asserts the DB row stores hashToken(token), §5 asserts the 5-failure lockout."
  },
  {
   "path": "src/lib/db.ts",
   "specification": "PURPOSE: Prisma client singleton. SIGNATURES: export const prisma: PrismaClient; export type PrismaTx = the transaction-client parameter type of prisma.$transaction. HOW: cache on globalThis in non-production to survive dev HMR; no query logging (avoids leaking payloads). HOW TO TEST: exercised by every integration case."
  },
  {
   "path": "src/lib/cache.ts",
   "specification": "PURPOSE: in-memory TTL cache for guidance and TTS payloads (single-instance assumption; NOT used for idempotency). SIGNATURES: export function cacheGet(key: string): string | null; export function cacheSet(key: string, value: string, ttlSeconds: number): void; export function cacheDel(key: string): void; export async function cachedJson<T>(key: string, ttlSeconds: number, produce: () => Promise<T>): Promise<{value: T; cacheHit: boolean}>; export function _resetCacheForTests(): void. HOW: Map<string,{value, expiresAt}>; cacheGet deletes-and-misses expired entries lazily; cacheSet evicts the entry with the earliest expiresAt when size ≥ 500 before inserting. cachedJson: hit → JSON.parse, {cacheHit true}; miss → produce(), store JSON.stringify, {cacheHit false}. EDGES: values are strings only; corrupt JSON on hit → treat as miss and overwrite. HOW TO TEST: infra cache section — round-trip; TTL expiry with vi.useFakeTimers; del; eviction cap; cachedJson hit flag."
  },
  {
   "path": "src/lib/rate-limit.ts",
   "specification": "PURPOSE: fixed-window in-memory rate limiting keyed by (bucket, client identity from getClientIp). SIGNATURES: export function enforceRateLimit(bucket: string, req: Request, opts?: {limit?: number; windowMs?: number}): void; export function rateLimitCheck(bucket: string, req: Request, limit: number, windowMs: number): void; export function rateLimitConsume(bucket: string, req: Request): void; export function _resetRateLimitForTests(): void. HOW: Map<string,{windowStart, count}> keyed bucket+'|'+ip; window rolls when now - windowStart ≥ windowMs. enforceRateLimit (defaults limit getRateLimitPerMinute(), windowMs 60000): count ≥ limit → AppError(429, 'RATE_LIMITED', 'Bạn thao tác quá nhanh. Vui lòng thử lại sau ít phút.', {retryAfterSeconds: ceil of window remainder}); else increment. rateLimitCheck throws the same 429 when count ≥ limit WITHOUT incrementing; rateLimitConsume increments (pair used by requireAdmin so only failures spend budget). Map capped at 10000 entries with oldest-window eviction. EDGES: identity 'global' (TRUST_PROXY off) means one shared bucket — deliberately fail-closed. HOW TO TEST: infra rate-limit section — N allowed then 429 with retryAfterSeconds; separate buckets independent; window reset via fake timers; TRUST_PROXY '0' → two different x-forwarded-for values share one bucket; '1' → last-entry identities tracked separately and prepended spoof entries ignored; check/consume pair semantics."
  },
  {
   "path": "src/lib/idempotency.ts",
   "specification": "PURPOSE: database-backed, exactly-once idempotency for mutating routes — the mutation and its idempotency record commit or roll back TOGETHER. SIGNATURES: export function canonicalJson(value: unknown): string (deterministic stringify with recursively sorted object keys); export function buildIdempotencyKey(parts: {operation: string; resourceId: string; sessionId?: string; messageId?: string; body?: unknown}): string | null; export async function withIdempotency(key: string | null, exec: (tx: PrismaTx) => Promise<{status: number; body: unknown}>): Promise<{status: number; body: unknown; replayed: boolean}>. HOW: buildIdempotencyKey → null when messageId falsy; else sha256hex(canonicalJson([operation, resourceId, sessionId ?? '', messageId, body ?? null])) — the body digest is embedded in the key, so one key can only ever correspond to one request payload (no separate digest column needed). withIdempotency: key null → prisma.$transaction(exec), replayed false. Key present → run prisma.$transaction(async tx => { tx.idempotencyRecord.create({key, httpStatus: 0, responseJson: {}}); const r = await exec(tx); tx.idempotencyRecord.update({where key, data {httpStatus: r.status, responseJson: r.body}}); return r; }). CREATE-FIRST is the serialization point: a concurrent duplicate blocks on the unique-key insert until the first transaction commits (then gets P2002) or aborts (then proceeds and executes). Catch Prisma P2002 on the create → read the committed record → return {status, body, replayed: true}. Any exec throw rolls back BOTH the mutation and the record → rethrow → a later retry re-executes cleanly (crash recovery, no stuck keys, no PENDING state observable). EDGES: exec must perform ALL its database writes through the provided tx; records are retained without TTL (stated assumption). Routes surface replays with header X-Idempotent-Replay 'true'. HOW TO TEST: infra — canonicalJson stable under key order; key null without messageId; identical parts → identical key; changing operation/resourceId/body → different keys. Integration §10 — exec that creates a Session row then throws leaves ZERO session rows and ZERO idempotency rows, and a retry with the same key succeeds exactly once; §2 — replay returns the stored body without re-running exec (row counts prove single execution) and concurrent duplicates (Promise.all) both return the same body with exactly one execution."
  },
  {
   "path": "src/lib/schema-guards.ts",
   "specification": "PURPOSE: fail-closed structural validation of ALL regulation content read from or written to the database (seed time, approval time, provider reads) — including the ReDoS gate. SIGNATURES: export type FieldDef = {id: string; type: 'text'|'textarea'|'number'|'date'|'select'|'radio'|'checkbox'|'file'|'province'; label: string; required?: boolean; options?: {value: string|boolean; label: string}[]; visibleWhen?: ConditionDef; placeholder?: string}; export type ConditionDef = {field: string; operator: 'equals'|'not_equals'|'in'|'greater_than'|'less_than'|'is_empty'|'not_empty'; value?: unknown}; export type RuleDef = {id: string; type: 'required'|'regex'|'date_not_future'|'date_after'|'number_range'|'conditional_required'|'cross_field_conflict'|'conditional_document'; fieldId?: string; params: Record<string, unknown>; message: string; suggestion: string; severity: 'error'|'warning'; orderNumber: number}; export type MigrationHint = {from: string; candidates: string[]}; export function isSafeRegex(pattern: string): boolean; export function parseFieldDefs(raw: unknown): FieldDef[]; export function parseRuleDefs(raw: unknown, fields: FieldDef[]): RuleDef[]; export function parseMigrationHints(raw: unknown): MigrationHint[]; export function safeHttpsUrl(url: unknown): string | null. HOW: violations throw AppError(500, 'DATA_INTEGRITY', 'Dữ liệu cấu hình không hợp lệ.', {reason}) — this is trusted-data verification, not user-input validation. parseFieldDefs: array ≤ 100; id slug ^[a-z][a-z0-9_]{0,49}$ unique; type in enum; label non-empty Vietnamese-capable string; options required and non-empty for select/radio; visibleWhen operator in enum and field references an existing id. isSafeRegex: string, length ≤ 200, safeRegex2(pattern) true (star-height ≤ 1 rejects nested quantifiers like (a+)+), AND new RegExp(pattern) compiles. parseRuleDefs: type in enum; fieldId (when required by type) must exist in fields; regex rules must satisfy isSafeRegex (parse-time REJECTION at seed/approval); messages/suggestions non-empty. safeHttpsUrl: new URL in try/catch, protocol === 'https:' → href, else null. HOW TO TEST: infra schema-guards section — valid marriage fixtures pass; duplicate id, unknown type, bad operator, missing options, non-compiling pattern, 201-char pattern, and the adversarial '(a+)+$' EACH throw DATA_INTEGRITY; safeHttpsUrl accepts https, rejects http/javascript/non-string."
  },
  {
   "path": "src/lib/rule-engine.ts",
   "specification": "PURPOSE: the pure deterministic validation core — condition evaluation, strict dates, sanitization, and full-form fail-closed rule execution. NO LLM, NO IO. SIGNATURES: export function isEmptyValue(v: unknown): boolean; export function coerceForComparison(v: unknown): unknown; export function evaluateCondition(cond: ConditionDef, data: Record<string, unknown>): boolean; export function isValidIsoDate(s: string): boolean; export function todayInVietnam(): string; export type ValidationErrorItem = {code: string; field?: string; fields?: string[]; message: string; suggestion: string; severity: 'error'|'warning'}; export function sanitizeFormData(fields: FieldDef[], data: unknown): {ok: true; sanitized: Record<string, unknown>} | {ok: false; issues: {field: string; code: string}[]}; export function runRules(fields: FieldDef[], rules: RuleDef[], data: Record<string, unknown>): ValidationErrorItem[]. HOW: isEmptyValue — '', null, undefined, [] empty; false and 0 NOT empty. coerceForComparison — trimmed numeric string → number; 'true'/'false' → boolean; else identity. evaluateCondition — coerce both sides; equals/not_equals strict after coercion; in: params array with PER-CANDIDATE coercion, NaN candidates never match; greater_than/less_than numeric only, any NaN → false; is_empty/not_empty via isEmptyValue; missing key = undefined. isValidIsoDate — ^\\d{4}-\\d{2}-\\d{2}$ then UTC Date round-trip equality (real calendar; leap years correct). todayInVietnam — now + 7h offset, sliced to YYYY-MM-DD (chosen semantics: Vietnam civil date, UTC+7, no DST). sanitizeFormData — data must be a plain object else INVALID_FORM_DATA issue; per key: unknown field id → UNKNOWN_FIELD; nested object/array → INVALID_VALUE_TYPE; number fields coerce numeric strings; checkbox/boolean-radio coerce 'true'/'false'; strings trimmed, > LIMITS.FIELD_VALUE_MAX → VALUE_TOO_LONG; returns a NEW object (never mutates input). runRules — rules sorted by orderNumber then id; errors appended in that order; message/suggestion come from the rule row. VISIBILITY SEMANTICS (chosen): required/regex/date_not_future/date_after/number_range/conditional_required are SKIPPED when their field's visibleWhen evaluates false; cross_field_conflict and conditional_document evaluate on raw data regardless of visibility (their purpose is contradiction/attachment checks). FAIL-CLOSED: required fires MISSING_REQUIRED when the key is ABSENT, not only when present-and-empty. Rule dispatch: required → isEmptyValue; regex → skip empty; value length > 500 → INVALID_FORMAT; re-check isSafeRegex at runtime and on failure emit RULE_CONFIG_INVALID severity 'warning' INSTEAD OF executing (defense in depth under the parse-time gate); else test. date_not_future → non-date string INVALID_DATE, value > todayInVietnam() DATE_IN_FUTURE; date_after {startField, endField} → skip when either empty, malformed side INVALID_DATE, end ≤ start DATE_ORDER_INVALID; number_range {min?, max?} → skip empty/non-numeric, OUT_OF_RANGE; conditional_required {when: ConditionDef} → condition true + empty → MISSING_REQUIRED; cross_field_conflict {fields, when: ConditionDef-combination as params {ifField, ifValue, thenField, thenNotEmpty:false}} → contradiction → CONFLICT with fields array and field undefined; conditional_document {when} → condition true + empty → MISSING_DOCUMENT, filename string passes. Rules referencing field ids absent from the schema are skipped (schema is authoritative). EDGES: every emitted item carries non-empty Vietnamese message and suggestion. HOW TO TEST: tests/rule-engine.test.ts is the authoritative lock, including the canonical 3-error payload defined THERE."
  },
  {
   "path": "src/lib/form-migration.ts",
   "specification": "PURPOSE: pure temporal version selection, ordered version comparison, activation transition, and never-guess data migration. NO IO. SIGNATURES: export type VersionLike = {id: string; version: string; status: string; effectiveFrom: Date | null; effectiveTo: Date | null}; export function compareVersions(a: string, b: string): number; export function selectActiveVersion<T extends VersionLike>(versions: T[], at: Date): T | null; export function activateVersion<T extends VersionLike>(versions: T[], targetVersion: string, now: Date): {changed: T[]; target: T; closed: T | null}; export function computeMigration(oldFields: FieldDef[], newFields: FieldDef[], hints: MigrationHint[], data: Record<string, unknown>, resolutions?: Record<string, string>): {migratedData: Record<string, unknown>; migrated: string[]; applied: {from: string; to: string}[]; needsConfirmation: {from: string; value: unknown; options: string[]}[]; dropped: string[]}. HOW: compareVersions — split on '.', numeric segment compare, missing segments = 0 (so '1.10' > '1.9'). selectActiveVersion — candidate iff status 'ACTIVE'-or-'RETIRED'... NO: chosen semantics — a version is selectable at instant `at` iff effectiveFrom !== null AND effectiveFrom ≤ at AND (effectiveTo === null OR at < effectiveTo) AND status !== 'DRAFT'; among matches return the one with the greatest effectiveFrom. DRAFT never selectable; effectiveFrom null never selectable; RETIRED versions remain resolvable inside their closed [from, to) interval — historical resolvability. Boundary semantics: inclusive start, exclusive end, so at === old effectiveTo selects the new version. activateVersion — target must exist (else AppError 400 FORM_VERSION_NOT_FOUND), must have status 'DRAFT', and compareVersions(target.version, currentActive.version) must be > 0 when a current active exists (else AppError 400 INVALID_TARGET_VERSION) — reactivating old/retired versions is impossible; returns copies with target {status ACTIVE, effectiveFrom now, effectiveTo null} and previous active {status RETIRED, effectiveTo now}; pure — does not mutate inputs. computeMigration — never mutates data; for each old field id: value empty → skip entirely; same id exists in newFields → copy into migratedData, push to migrated; else a hint with from === id → options = hint.candidates filtered to ids present in newFields; options empty → dropped; resolutions[id] present → must be within options else AppError(400, 'INVALID_RESOLUTION', vi message) → copy value to that id, push {from, to} to applied; no resolution → push {from, value, options} to needsConfirmation and DO NOT place the value anywhere; no hint → dropped. New-only fields absent from migratedData. HOW TO TEST: tests/form-versioning.test.ts and tests/migration.test.ts are the authoritative locks."
  },
  {
   "path": "src/lib/intake-machine.ts",
   "specification": "PURPOSE: pure guided-intake state machine, answer validation, pruning, and grounded guidance assembly. SIGNATURES: export type QuestionRow = {code: string; orderNumber: number; fieldType: 'radio'|'select'|'text'|'province'; options: {value: string|boolean; label: string}[] | null; condition: ConditionDef | null; questionText: string}; export function computeQuestionFlow(questions: QuestionRow[], answers: Record<string, unknown>): {next: QuestionRow | null; answered: number; total: number}; export function pruneAnswers(questions: QuestionRow[], answers: Record<string, unknown>): {answers: Record<string, unknown>; removed: string[]}; export function validateAnswer(q: QuestionRow, value: unknown): unknown; export function buildGuidance(input: {procedure: {code; name; agency; sourceUrl; lastCheckedAt}; procedureVersion: {version; stepsJson; durationText; feesText}; documents: DocumentRow[]; answers: Record<string, unknown>}): Guidance. HOW: applicable(q) = !q.condition || evaluateCondition(q.condition, answers); computeQuestionFlow sorts by orderNumber, total = applicable count, answered = applicable with key present, next = first applicable unanswered or null. pruneAnswers: drop keys with no matching question; iterate to FIXPOINT removing answers whose question became inapplicable (dependent chains fully pruned); return removed list. validateAnswer: radio/select — coerce 'true'/'false' to boolean when options are boolean-valued, then the value must strictly equal one option value else AppError(400, 'INVALID_ANSWER', vi, {questionCode}); text — non-empty trimmed string ≤ LIMITS.ANSWER_TEXT_MAX else INVALID_ANSWER; province — must be in PROVINCES else INVALID_ANSWER; returns the stored (coerced) value. buildGuidance: documents in orderNumber order; unconditional always included; conditional included iff evaluateCondition true, carrying reason = reasonText; output {procedure block (code, name, agency, sourceUrl, version, lastCheckedAt), checklist [{code, name, originals, copies, reason?}], steps, durationText, feesText, disclaimer: DISCLAIMER}. HOW TO TEST: tests/intake-machine.test.ts is the authoritative lock (ordering, condition gating, preset skipping, fixpoint pruning, coercion, INVALID_ANSWER, DIVORCE_DOCUMENT reason string, strict DISCLAIMER equality)."
  },
  {
   "path": "src/lib/data-provider.ts",
   "specification": "PURPOSE: the IProcedureDataProvider boundary — the ONLY reader of regulation content; every read passes schema-guards so malformed DB content fails closed. SIGNATURES: export interface IProcedureDataProvider {getProcedure(code): Promise<ProcedureDto | null>; listProcedures(): Promise<ProcedureDto[]>; getActiveProcedureVersion(procedureId): Promise<ProcedureVersionDto | null>; getClarifyingQuestions(procedureId): Promise<QuestionRow[]>; getDocuments(procedureId): Promise<DocumentRow[]>; getForm(code): Promise<FormDto | null>; getFormVersion(formCode, version): Promise<FormVersionDto | null>; getActiveFormVersion(formCode, at?: Date): Promise<FormVersionDto | null>; getValidationRules(formVersionId): Promise<RuleDef[]>; getCatalogOverview(): Promise<CatalogOverviewDto>}; export function getProvider(): IProcedureDataProvider. HOW: PrismaProcedureDataProvider maps rows to DTOs: FormVersionDto = {id, formId, formCode, version, status, effectiveFrom, effectiveTo, fields: parseFieldDefs(schemaJson.fields), migrationHints: parseMigrationHints(schemaJson.migrationHints ?? [])}; questions via parse of optionsJson/conditionJson (operator whitelist reuse); getActiveFormVersion loads ALL versions of the form and applies selectActiveVersion(versions, at ?? new Date()) — computed per call, NEVER cached, so approval takes effect immediately; getCatalogOverview returns procedures with agency/sourceUrl/lastCheckedAt, active procedure version, and each form's versions {version, status, effectiveFrom, effectiveTo} for the sources page and admin overview. getProvider returns a module singleton. EDGES: guard failures propagate as 500 DATA_INTEGRITY (correct: corrupt regulation data must not render). HOW TO TEST: exercised throughout integration; guards covered in infra."
  },
  {
   "path": "src/lib/ai/usage.ts",
   "specification": "PURPOSE: AI observability — usage logging and the admin cost summary. SIGNATURES: export function logAiUsage(entry: {serviceType: 'llm'|'stt'|'tts'; model: string; promptTokens?: number; completionTokens?: number; audioSeconds?: number; latencyMs: number; cacheHit?: boolean; degraded?: boolean}): void; export async function getUsageSummary(): Promise<UsageSummary>. HOW: logAiUsage is fire-and-forget — prisma.aiUsageLog.create(...).catch(console.error) so logging can never fail a request; NO text content parameter exists (transcript non-persistence by construction). getUsageSummary groups by serviceType: calls, tokens, audioSeconds, avg latency, cacheHits, degradedCount, estimatedCostUsd from a static price table (mock models cost 0; gpt-4o-mini/whisper-1/tts-1 approximate published rates). HOW TO TEST: integration §8 asserts stt/tts/llm rows exist with only numeric/flag fields and that the tts cache hit is logged."
  },
  {
   "path": "src/lib/ai/upstream.ts",
   "specification": "PURPOSE: the single hardened outbound HTTP path for live AI providers — bounded, validated, SSRF-policy-aware. SIGNATURES: export class UpstreamError extends Error {constructor(reason: string)}; export async function fetchUpstreamJson(path: string, init: {method; headers?; body}, opts?: {timeoutMs?: number; maxBytes?: number}): Promise<unknown>; export async function fetchUpstreamBinary(path: string, init, opts?: {timeoutMs?; maxBytes?; contentTypePrefix?: string}): Promise<{buffer: Buffer; contentType: string}>. HOW: URL = getOpenAiBaseUrl() (already scheme/host-policy validated by config) + path, where path is ALWAYS a fixed literal from our providers ('/chat/completions', '/audio/transcriptions', '/audio/speech') — never derived from user input. Both functions: Authorization Bearer getOpenAiKey(); AbortController timeout default 30000ms; response !ok → UpstreamError with status (body discarded). fetchUpstreamJson: content-type must include 'application/json'; enforce Content-Length ≤ maxBytes (default 1 MiB) when present and re-check the read text length; JSON.parse in try/catch → UpstreamError. fetchUpstreamBinary: content-type must start with contentTypePrefix (default 'audio/'); maxBytes default 10 MiB checked on header and on buffered length. EDGES: throws UpstreamError only — callers (routes) translate into mock fallback + degraded=true; never leaks the API key or raw upstream bodies into AppError envelopes. HOW TO TEST: infra upstream section with vi.stubGlobal('fetch', ...) — non-2xx, wrong content-type, oversized Content-Length, unparseable JSON each throw UpstreamError; happy path returns parsed value; timeout path aborts (fake timers)."
  },
  {
   "path": "src/lib/ai/llm.ts",
   "specification": "PURPOSE: intent classification and error-code explanation — the ONLY two LLM capabilities, both grounded. SIGNATURES: export interface LlmProvider {readonly name: string; classifyIntent(message: string, catalog: {code: string; name: string}[]): Promise<{procedureCode: string | null; confidence: number}>; explainErrors(errors: {code: string; field?: string; fields?: string[]}[], formCode: string): Promise<string>}; export function classifyByKeywords(message: string): {procedureCode: string; confidence: number} | null; export function getLlmProvider(): LlmProvider; export const mockLlm: LlmProvider. HOW: classifyByKeywords — fold input: lowercase, NFD normalize, strip combining marks, đ→d; static keyword table {MARRIAGE_REGISTRATION: ['dang ky ket hon', 'ket hon', 'dam cuoi', 'giay ket hon'], BIRTH_REGISTRATION: ['dang ky khai sinh', 'khai sinh', 'giay khai sinh']}; LONGEST matching keyword wins; hit → confidence 0.95; empty/no match → null. MockLlmProvider (name 'mock'): classifyIntent delegates to classifyByKeywords else {null, 0}; explainErrors renders a deterministic Vietnamese template mapping each code (+field ids) to one plain-language sentence, prefixed 'Giải thích: '. OpenAiLlmProvider (name 'openai'): fetchUpstreamJson('/chat/completions', {model LLM_MODEL, temperature 0, response_format json_object, messages: system prompt in Vietnamese instructing: choose procedureCode ONLY from the provided catalog or null, output strict JSON {procedureCode, confidence 0..1}, do not invent procedures — for explainErrors: explain ONLY the given error codes in simple Vietnamese for elderly users, ≤ 3 sentences, no legal advice}); validate the reply shape: procedureCode must be null or ∈ catalog codes (else null), confidence clamped to [0,1], explanation must be a non-empty string ≤ 2000 chars (else UpstreamError); logAiUsage with token counts from the response when present. getLlmProvider: AI_PROVIDER 'openai' → OpenAi, else mock. EDGES: explainErrors input NEVER contains user values or legal text — routes construct the reduced array. HOW TO TEST: infra intent section (fold matrix, longest-match, empty → null); mock explainErrors determinism; upstream shape-validation via stubbed fetch."
  },
  {
   "path": "src/lib/ai/audio.ts",
   "specification": "PURPOSE: hardened audio inspection — the single gate every uploaded byte passes before STT. SIGNATURES: export async function inspectAudio(buf: Buffer): Promise<{mimeType: string; durationSeconds: number}>. HOW: parseBuffer from music-metadata inside try/catch — any parser throw → AppError(400, 'UNSUPPORTED_AUDIO_TYPE', 'Định dạng âm thanh không được hỗ trợ.'). Map metadata.format.container (case-insensitive contains) to the allowlist: WAVE→audio/wav, Ogg→audio/ogg, EBML/Matroska/WebM→audio/webm, MPEG→audio/mpeg, MP4/M4A/isom→audio/mp4; anything else → UNSUPPORTED_AUDIO_TYPE. duration = metadata.format.duration; undefined, non-finite, or ≤ 0 → AppError(400, 'AUDIO_UNREADABLE', vi message). Return {mimeType, durationSeconds: duration}. This replaces hand-rolled magic-byte/RIFF parsing entirely — chunk-walking, forged sizes, and NaN byte rates are the library's hardened problem, and duration is derived from actual frames for ALL supported formats, closing the compressed-format duration gap. HOW TO TEST: integration §7 — PNG bytes → UNSUPPORTED_AUDIO_TYPE; truncated/garbage RIFF header → 400; a small valid PCM WAV built in-test parses with duration ≈ samples/rate; the mock TTS WAV round-trips through inspectAudio."
  },
  {
   "path": "src/lib/ai/stt.ts",
   "specification": "PURPOSE: speech-to-text gateway. SIGNATURES: export interface SttProvider {readonly name: string; transcribe(audio: Buffer, mimeType: string, language: string, durationSeconds: number): Promise<{text: string; model: string; audioSeconds: number}>}; export function getSttProvider(): SttProvider; export const mockStt: SttProvider. HOW: MockSttProvider (name 'mock') returns the canned transcript 'Tôi muốn đăng ký kết hôn', model 'mock-stt', audioSeconds = the durationSeconds argument (already parser-derived — both providers bill identically). OpenAiSttProvider posts multipart FormData (file Blob with the sniffed mimeType, model STT_MODEL, language) via fetchUpstreamJson('/audio/transcriptions'); response must contain a string 'text' ≤ 10000 chars else UpstreamError. Both time the call and logAiUsage({serviceType 'stt', audioSeconds, latencyMs}). EDGES: providers never persist audio or text; live failure is handled by the ROUTE (mock fallback, degraded=true). HOW TO TEST: integration §7."
  },
  {
   "path": "src/lib/ai/tts.ts",
   "specification": "PURPOSE: text-to-speech gateway with a deterministic mock and the unambiguous cache-key builder. SIGNATURES: export const TTS_VOICES = ['vi-female', 'vi-male'] as const; export interface TtsProvider {readonly name: string; synthesize(text: string, voice: string, speed: number, language: string): Promise<{audio: Buffer; mimeType: string; model: string; audioSeconds: number}>}; export function getTtsProvider(): TtsProvider; export const mockTts: TtsProvider; export function makeTtsCacheKey(text: string, voice: string, speed: number, language: string): string. HOW: makeTtsCacheKey = 'tts:' + sha256hex(JSON.stringify([text, voice, speed, language])) — array-JSON encoding is injective over the tuple, so 'a|b'/'c' can never collide with 'a'/'b|c' (fixes the delimiter-ambiguity flaw); parameter allowlisting itself lives in the route. MockTtsProvider (name 'mock'): builds a fully valid mono 16-bit PCM WAV at 8000 Hz in code — duration = clamp(text.length * 0.05, 0.5, 3) s; sine frequency = 200 + (parseInt(first 8 hex chars of sha256hex(text), 16) % 400) Hz; byte-identical output for identical input; mimeType 'audio/wav', model 'mock-tts'. OpenAiTtsProvider: fetchUpstreamBinary('/audio/speech', {model TTS_MODEL, input text, voice, speed}) with contentTypePrefix 'audio/'; mimeType from upstream, model TTS_MODEL, audioSeconds estimated text.length * 0.05. Both logAiUsage (cacheHit false; the ROUTE logs cache hits). HOW TO TEST: infra — makeTtsCacheKey collision pair differs, identical tuple equal; integration §8 — identical calls byte-identical, mock WAV survives inspectAudio.\n\nREVISION (reviewer feedback — MUST implement): OpenAiTtsProvider MUST map public voice ids to upstream OpenAI voices before the API call: 'vi-female' -> 'nova', 'vi-male' -> 'onyx' (OpenAI only accepts alloy/echo/fable/onyx/nova/shimmer; sending 'vi-female' verbatim 400s and silently degrades live TTS to the mock beep). The route-level allowlist of public ids stays unchanged; the mapping lives inside the provider."
  },
  {
   "path": "src/app/api/v1/procedures/search/route.ts",
   "specification": "PURPOSE: intent endpoint (AI stage 1). SIGNATURES: export const POST = handleRoute(async (req) => ...). HOW: enforceRateLimit('search', req); body via readJsonBody; message = requireString(body, 'message', 1000); province = optionalString(body, 'province', 100). Stage 1: classifyByKeywords(message); on hit load provider.getProcedure(code) + getActiveProcedureVersion → 200 {procedure: {code, name, sector, agency, sourceUrl, version, lastCheckedAt}, confidence, matchedBy: 'keyword', province: province ?? null, ...aiMeta('mock-keyword' → use provider-independent 'rule', degraded false)} — use aiMode 'rule' for the deterministic path. Stage 2 (keyword miss only): catalog = provider.listProcedures() mapped to {code, name}; getLlmProvider().classifyIntent(message, catalog) in try/catch — UpstreamError/any throw → mockLlm.classifyIntent with degraded = true; result with procedureCode non-null AND confidence ≥ 0.6 → 200 as above with matchedBy 'llm' and the live/mock aiMeta; otherwise AppError(404, 'PROCEDURE_NOT_FOUND', 'Chưa nhận diện được thủ tục. Hiện hỗ trợ: Đăng ký kết hôn, Đăng ký khai sinh.'). EDGES: province is advisory only (echoed for preset). HOW TO TEST: integration §1; rate limit in §9."
  },
  {
   "path": "src/app/api/v1/guided-intake/start/route.ts",
   "specification": "PURPOSE: create an intake session and issue its capability token exactly once. SIGNATURES: export const POST = handleRoute(...). HOW: enforceRateLimit('intake-start', req); body {procedureCode: requireString, presetAnswers?: optionalObject, intentMessage?: optionalString(1000)}. provider.getProcedure → null: 404 PROCEDURE_NOT_FOUND. questions = provider.getClarifyingQuestions. PRESET VALIDATION IS STRICT (no silent drops): presetAnswers defined → must have ≤ 20 keys; any key not matching a question code → AppError(400, 'UNKNOWN_QUESTION', vi, {questionCode: key}); each value passes validateAnswer (INVALID_ANSWER 400 with details propagates). answers = pruneAnswers(questions, validated presets).answers. token = generateAccessToken(); prisma.session.create({accessTokenHash: hashToken(token), procedureCode, intent: intentMessage ?? null, answersJson: answers, expiresAt: now + getSessionTtlHours() hours}). flow = computeQuestionFlow → 201 {sessionId, accessToken: token, procedure: {code, name}, done: flow.next === null, question: flow.next, progress: {answered, total}, disclaimer: DISCLAIMER}. The plaintext token appears ONLY in this response body — never persisted, never logged. HOW TO TEST: integration §2 — preset province skips its question and total shrinks; invalid preset value → 400 INVALID_ANSWER; unknown preset key → 400 UNKNOWN_QUESTION; DB row stores hashToken(token)."
  },
  {
   "path": "src/app/api/v1/guided-intake/[sessionId]/answer/route.ts",
   "specification": "PURPOSE: record one answer, return the next DB-driven question — never an LLM call, exactly-once under retries. SIGNATURES: export const POST = handleRoute(async (req, {params}: {params: {sessionId: string}}) => ...). HOW: enforceRateLimit('intake-answer', req); load session by id → 404 SESSION_NOT_FOUND; expiresAt < now → 404 SESSION_EXPIRED; requireSessionToken(req, session.accessTokenHash); body {questionCode: requireString, value: required (any JSON scalar), messageId: optionalString}. key = buildIdempotencyKey({operation 'intake-answer', resourceId: sessionId, sessionId, messageId, body: {questionCode, value}}). Wrap in a retry loop (≤ 3 attempts) around withIdempotency(key, exec) where exec(tx): re-read the session VIA tx (fresh answers + updatedAt); find the question among provider.getClarifyingQuestions else AppError(400, 'UNKNOWN_QUESTION'); stored = validateAnswer(q, value); merged = {...fresh.answers, [code]: stored}; pruned = pruneAnswers(questions, merged); tx.session.updateMany({where: {id, updatedAt: freshUpdatedAt}, data: {answersJson: pruned.answers, currentStep: flow.answered}}) — count 0 → throw AppError(409, 'CONCURRENT_UPDATE') (rolls back the whole idempotency transaction, safe to retry); flow = computeQuestionFlow(questions, pruned.answers); return {status 200, body {done: flow.next === null, question: flow.next, progress: {answered, total}, removedAnswers: pruned.removed}}. Retry loop retries ONLY on CONCURRENT_UPDATE, then rethrows the 409. Response adds header X-Idempotent-Replay 'true' when replayed. HOW TO TEST: integration §2 — four marriage answers reach done=true (has_foreign_element false, previously_married true, submission_channel online after preset province); duplicate messageId replays the identical body with the header and exec runs once; wrong token 403; unknown question 400."
  },
  {
   "path": "src/app/api/v1/guided-intake/[sessionId]/guidance/route.ts",
   "specification": "PURPOSE: grounded guidance payload for the checklist screen. SIGNATURES: export const GET = handleRoute(async (req, {params}) => ...). HOW: enforceRateLimit('guidance', req); load session (404/expired ordering per conventions); requireSessionToken; procedure = provider.getProcedure(session.procedureCode) and procVersion = getActiveProcedureVersion → 404 PROCEDURE_NOT_FOUND when either missing; documents = provider.getDocuments; answers = pruneAnswers(questions, session.answers).answers; key = 'guidance:' + procedure.code + ':' + procVersion.version + ':' + sha256hex(canonicalJson(answers)) + ':vi'; {value, cacheHit} = cachedJson(key, 300, () => buildGuidance({procedure, procedureVersion, documents, answers})); 200 {...value, meta: {cached: cacheHit}}. EDGES: cache key uses canonicalJson so answer-key ordering cannot fork entries. HOW TO TEST: integration §3 — previously_married=true includes DIVORCE_DOCUMENT with its exact reason; disclaimer strictly equals DISCLAIMER; second call meta.cached true.\n\nREVISION (reviewer feedback — MUST implement): The guidance payload additionally carries legalBasisText, each step's 'example', each document's submissionType, and formAvailable: boolean (false when the procedure has no ACTIVE FormVersion — guidance-only procedures must flow through search -> intake -> checklist without any 500/404)."
  },
  {
   "path": "src/app/api/v1/forms/[formCode]/active/route.ts",
   "specification": "PURPOSE: serve the currently active form version by effective dates. SIGNATURES: export const GET = handleRoute(async (req, {params}) => ...). HOW: enforceRateLimit('forms-active', req); v = provider.getActiveFormVersion(params.formCode) → null: 404 FORM_NOT_FOUND; 200 {formCode, version: v.version, effectiveFrom, fields: v.fields} — migrationHints intentionally absent. Resolution is computed per request (provider never caches), so approval flips this response immediately. HOW TO TEST: integration §5 — '1.0' before approval, '2.0' with permanent_address/temporary_address/phone_number after, zero frontend edits."
  },
  {
   "path": "src/app/api/v1/forms/[formCode]/validate/route.ts",
   "specification": "PURPOSE: server-side full-form validation (pure rule engine — NO LLM in the decision) plus AI explanation of the resulting CODES, with version-access control. SIGNATURES: export const POST = handleRoute(async (req, {params}) => ...). HOW: enforceRateLimit('validate', req); body {formVersion: requireString, data: required object, applicationId?: optionalString}. requested = provider.getFormVersion(formCode, formVersion) → 404 FORM_VERSION_NOT_FOUND. ACCESS CONTROL: active = provider.getActiveFormVersion(formCode); if !active || requested.id !== active.id → anonymous access DENIED: applicationId must be present, the application (with its session) must exist, application.formVersionId === requested.id, and requireSessionToken(req, session.accessTokenHash) must pass — any miss → AppError(403, 'VERSION_NOT_ACCESSIBLE', 'Phiên bản biểu mẫu này không khả dụng.'); DRAFT versions are never pinned by applications, so they are unreachable. Then: sanitizeFormData(requested.fields, data) not ok → AppError(400, 'INVALID_FORM_DATA', vi, {issues}); rules = provider.getValidationRules(requested.id); errors = runRules(fields, rules, sanitized). AI stage: when errors non-empty, reduced = errors.map to {code, field, fields} ONLY (values structurally impossible); explanation = getLlmProvider().explainErrors(reduced, formCode) in try/catch → fallback mockLlm with degraded=true. 200 {valid: errors.length === 0, errors, aiExplanation (only when invalid), formCode, formVersion, ...aiMeta}. No idempotency (read-only apart from usage logs). HOW TO TEST: integration §4 — canonical payload → exactly the 3 ordered errors with Vietnamese messages and non-empty aiExplanation under mock; anonymous validate of DRAFT '2.0' → 403; after approval, anonymous '1.0' → 403 but with owning applicationId + token → 200."
  },
  {
   "path": "src/app/api/v1/applications/route.ts",
   "specification": "PURPOSE: create a DRAFT application pinned to the active form version, owned by the session, exactly-once. SIGNATURES: export const POST = handleRoute(...). HOW: enforceRateLimit('applications', req); body {sessionId: requireString, messageId?: optionalString}; load session → 404 SESSION_NOT_FOUND / SESSION_EXPIRED; requireSessionToken; active = provider.getActiveFormVersion(session.procedureCode-mapped form code — form code equals procedure code by seed convention) → 404 FORM_NOT_FOUND. prefill: pruned session answers restricted to keys that are active field ids, passed through sanitizeFormData with any issue-carrying keys dropped. key = buildIdempotencyKey({operation 'application-create', resourceId: sessionId, sessionId, messageId, body: {}}); withIdempotency(key, exec) where exec(tx): tx.application.create({sessionId, formVersionId: active.id, status 'DRAFT', dataJson: prefill}) → {status 201, body {applicationId, formCode, formVersion: active.version, status: 'DRAFT', data: prefill, revision: 0}}. Respond with the stored status and X-Idempotent-Replay when replayed. HOW TO TEST: integration §4 — application from the finished session carries province + previously_married prefilled at version '1.0' pre-approval; same messageId twice creates exactly one row."
  },
  {
   "path": "src/app/api/v1/applications/[id]/route.ts",
   "specification": "PURPOSE: read and save one application — the form page's only data source (provider boundary preserved) with optimistic concurrency on save. SIGNATURES: export const GET and export const PUT, both handleRoute-wrapped with {params: {id}}. SHARED: enforceRateLimit('applications-id', req); load application including its FormVersion (mapped via provider.getFormVersion by ids: fetch the application row, then resolve its pinned FormVersionDto through the provider by formVersionId — provider gains a getFormVersionById(id) convenience OR resolve via stored formCode+version; choose getFormVersionById added to the interface) and its session → 404 APPLICATION_NOT_FOUND; requireSessionToken(req, session.accessTokenHash). GET: pinned = FormVersionDto (fields guarded, hints stripped); rules = provider.getValidationRules(pinned.id); newer = provider.getActiveFormVersion(formCode); updateAvailable = !!newer && compareVersions(newer.version, pinned.version) > 0; 200 {applicationId, formCode, formVersion: pinned.version, status, data, revision, fields, rules, updateAvailable, newVersion: newer?.version when updateAvailable, procedure: {name, sourceUrl, lastCheckedAt}}. PUT: body {data: required object, revision: required number}; sanitizeFormData against the PINNED fields → not ok: 400 INVALID_FORM_DATA {issues} (unknown keys and wrong types rejected); prisma.application.updateMany({where: {id, revision: body.revision}, data: {dataJson: sanitized, revision: body.revision + 1}}) — count 0 → AppError(409, 'CONCURRENT_UPDATE', 'Dữ liệu đã được thay đổi ở nơi khác. Vui lòng tải lại.'); 200 {applicationId, formCode, formVersion, status, data: sanitized, revision: body.revision + 1}. HOW TO TEST: integration §4 — wrong token 403; PUT with unknown key 400 naming it; stale revision → 409; saved values round-trip with incremented revision."
  },
  {
   "path": "src/app/api/v1/applications/[id]/migrate/route.ts",
   "specification": "PURPOSE: two-phase, non-destructive, race-safe migration to the newer active form version — the row is untouched until every ambiguity is user-resolved, and concurrent saves cannot be silently lost. SIGNATURES: export const POST = handleRoute(async (req, {params}) => ...). HOW: enforceRateLimit('migrate', req); load + auth as in applications/[id]; body {confirm?: boolean, resolutions?: Record<string,string>}; active = provider.getActiveFormVersion(formCode); !active OR active.id === pinned.id → 409 NO_NEWER_VERSION. PREVIEW (confirm falsy): computeMigration(pinned.fields, active.fields, active.migrationHints, application.data, undefined) with NO writes → 200 {mode 'preview', fromVersion, toVersion, migrated, needsConfirmation, dropped}. COMMIT (confirm true): prisma.$transaction(async tx => { fresh = tx.application.findUnique (RE-READ INSIDE the transaction — the migration source of truth is current data, so a save that landed after preview is preserved, not clobbered); fresh.formVersionId !== pinned.id → AppError(409, 'NO_NEWER_VERSION') (already migrated concurrently); result = computeMigration(pinned.fields, active.fields, hints, fresh.dataJson, resolutions ?? {}) — INVALID_RESOLUTION propagates as 400; result.needsConfirmation non-empty → AppError(400, 'MISSING_RESOLUTION', vi, {unresolved: from ids}); tx.application.updateMany({where: {id, revision: fresh.revision}, data: {formVersionId: active.id, dataJson: result.migratedData, revision: fresh.revision + 1}}) — count 0 → AppError(409, 'CONCURRENT_UPDATE'); }); 200 {mode 'committed', fromVersion, toVersion, migrated, applied, dropped, revision, message: 'Biểu mẫu đã được cập nhật theo quy định mới.'}. HOW TO TEST: integration §6 — preview leaves the row unchanged (re-GET still 1.0 with residence intact); confirm without resolutions → 400 MISSING_RESOLUTION; a PUT between preview and commit is reflected in the committed migratedData; confirm with {residence: 'permanent_address'} commits, permanent_address carries the latest saved value; repeat migrate → 409 NO_NEWER_VERSION; bogus resolution target → 400 INVALID_RESOLUTION."
  },
  {
   "path": "src/app/api/v1/speech/transcribe/route.ts",
   "specification": "PURPOSE: server STT gateway (explicit mic action only), size-capped BEFORE parsing, duration-capped for ALL formats, transcript never persisted. SIGNATURES: export const POST = handleRoute(...). HOW (ordered): enforceRateLimit('transcribe', req). PRE-PARSE CAP: cl = req.headers.get('content-length'); when present and parseInt(cl) > LIMITS.AUDIO_PREPARSE_MAX_BYTES → AppError(413, 'AUDIO_TOO_LARGE', vi) WITHOUT touching the body (platforms and the Render proxy always set Content-Length for uploads; the residual streaming case is bounded by the deployment platform's request-size limit — stated assumption). form = await req.formData() in try/catch → 400 INVALID_MULTIPART. file = form.get('audio'); not a File → 400 AUDIO_REQUIRED; file.size > LIMITS.AUDIO_MAX_BYTES → 413 AUDIO_TOO_LARGE (post-parse recheck, authoritative). buffer = Buffer.from(await file.arrayBuffer()); {mimeType, durationSeconds} = await inspectAudio(buffer) (server-side sniffing — the client MIME string is ignored); durationSeconds > LIMITS.AUDIO_MAX_SECONDS → 400 AUDIO_TOO_LONG ('Đoạn ghi âm vượt quá 60 giây.') — enforced for EVERY accepted format because inspectAudio derives duration from actual frames. Any legacy client field like durationSeconds in the form is IGNORED entirely (accounting comes from the parser). result = getSttProvider().transcribe(buffer, mimeType, 'vi', durationSeconds) in try/catch — any throw (incl. UpstreamError) → mockStt.transcribe with degraded = true. 200 {text, model, audioSeconds, language: 'vi', ...aiMeta}. PRIVACY: the transcript exists only in this response — no session/application write, and AiUsageLog has no text column. HOW TO TEST: integration §7 — Content-Length 7MB header → 413 before parsing; PNG bytes → 400 UNSUPPORTED_AUDIO_TYPE; garbage RIFF → 400; tiny valid WAV → 200 with the canned mock transcript; DB gains only an stt usage row with numeric/flag fields."
  },
  {
   "path": "src/app/api/v1/speech/synthesize/route.ts",
   "specification": "PURPOSE: server TTS gateway with strict parameter allowlists and content-addressed caching. SIGNATURES: export const POST = handleRoute(...). HOW: enforceRateLimit('synthesize', req); body via readJsonBody; text = requireString(body, 'text', LIMITS.TTS_TEXT_MAX) — over-length surfaces as 400 VALUE_TOO_LONG per http.ts, and an explicit check maps it to 400 TEXT_TOO_LONG for contract clarity; voice = optionalString ?? 'vi-female', MUST be in TTS_VOICES else 400 INVALID_INPUT {field 'voice'}; speed = body.speed ?? 1, must be a finite number in [0.5, 2] else 400 INVALID_INPUT {field 'speed'}; language = optionalString ?? 'vi', must equal 'vi' else 400 INVALID_INPUT {field 'language'}. key = makeTtsCacheKey(text, voice, speed, language) (unambiguous JSON-array hash; allowlists above close the remaining ambiguity vector). cacheGet(key) HIT → JSON.parse {b64, mimeType, model}; logAiUsage({serviceType 'tts', model, cacheHit true, latencyMs}); respond Buffer.from(b64, 'base64'). MISS → getTtsProvider().synthesize(...) in try/catch → mockTts fallback with degraded = true (provider logs its own usage); cacheSet(key, JSON.stringify({b64, mimeType, model}), 3600); respond. Response: raw audio body, headers Content-Type mimeType, X-Cache-Hit 'true'|'false', X-Model, and X-Degraded 'true' when degraded. HOW TO TEST: integration §8 — invalid voice/speed/language → 400; identical calls: first X-Cache-Hit false, second true with byte-identical bodies; ai_usage_logs gains a cacheHit=true tts row; mock output round-trips through inspectAudio."
  },
  {
   "path": "src/app/api/v1/admin/change-requests/route.ts",
   "specification": "PURPOSE: admin list of change requests. SIGNATURES: export const GET = handleRoute(...). HOW: requireAdmin(req) (constant-time compare + failure limiter per auth.ts); prisma.changeRequest.findMany include oldVersion → form, orderBy createdAt desc; 200 {changeRequests: [{id, status, sourceUrl, diff: diffJson, proposedTargetVersion: proposedDataJson.targetVersion, oldVersion: {formCode: form.code, version}, createdAt, reviewedBy, reviewedAt}]}. HOW TO TEST: integration §5 — valid token returns the seeded PENDING request; missing token 401; five bad tokens then 429."
  },
  {
   "path": "src/app/api/v1/admin/change-requests/[id]/approve/route.ts",
   "specification": "PURPOSE: the regulatory-update switch — activates the proposed form version atomically, serialized PER FORM, with target validity enforced. SIGNATURES: export const POST = handleRoute(async (req, {params}) => ...). HOW: requireAdmin(req); prisma.$transaction(async tx => { (1) CLAIM: tx.changeRequest.updateMany({where: {id, status: 'PENDING'}, data: {status: 'APPROVED', reviewedBy: 'shared-admin-token', reviewedAt: now}}); count 0 → re-read: row exists → AppError(409, 'ALREADY_PROCESSED') else 404 CHANGE_REQUEST_NOT_FOUND. (2) load the CR with oldVersion → form; targetVersion = proposedDataJson.targetVersion. (3) PER-FORM SERIALIZATION: read form.revision via tx, then tx.form.updateMany({where: {id: formId, revision}, data: {revision: revision + 1}}); count 0 → AppError(409, 'CONCURRENT_UPDATE') — under Postgres read-committed a concurrent approver blocks on this row lock until the winner commits, then re-evaluates the WHERE with the bumped revision and loses; the rollback also restores its CR claim to PENDING. (4) RE-READ all FormVersions of the form via tx (fresh, post-lock state). (5) activateVersion(versions, targetVersion, now) — throws 400 FORM_VERSION_NOT_FOUND / 400 INVALID_TARGET_VERSION unless the target exists, is DRAFT, and is strictly newer than the current active (reactivating old or retired versions is impossible). (6) persist each changed row via tx.formVersion.update (target: ACTIVE, effectiveFrom now, effectiveTo null; previous active: RETIRED, effectiveTo now). (7) INVARIANT: count versions with status ACTIVE for this form === 1 else throw DATA_INTEGRITY (rolls everything back). }); 200 {changeRequestId, status: 'APPROVED', formCode, activatedVersion, closedVersion, effectiveFrom}. Because active resolution is computed per request, GET /forms/{code}/active serves the new version immediately. HOW TO TEST: integration §5 — approve flips active to 2.0; sequential re-approve 409 ALREADY_PROCESSED; two CONCURRENT approves of two PENDING CRs targeting the same form yield exactly one 200 and one 4xx with exactly one ACTIVE version remaining; a CR targeting a non-DRAFT or older version → 400 INVALID_TARGET_VERSION."
  },
  {
   "path": "src/app/api/v1/admin/overview/route.ts",
   "specification": "PURPOSE: admin console data source (keeps the /admin page free of unauthenticated server-side data). SIGNATURES: export const GET = handleRoute(...). HOW: requireAdmin(req); 200 {procedures: await getProvider().getCatalogOverview(), usage: await getUsageSummary()}. HOW TO TEST: integration §5 — 401 without token; after approval the versions table shows v2.0 ACTIVE and v1.0 RETIRED; usage section carries llm/stt/tts aggregates."
  },
  {
   "path": "src/app/api/openapi/route.ts",
   "specification": "PURPOSE: serve the contract file (outside /api/v1 so the v1 surface stays exactly 15 endpoints). SIGNATURES: export const GET = handleRoute(async () => ...). HOW: module-level lazy-cached readFileSync(join(process.cwd(), 'openapi.yaml'), 'utf8'); Response with Content-Type 'application/yaml'. HOW TO TEST: manual curl returns the YAML."
  },
  {
   "path": "src/app/layout.tsx",
   "specification": "PURPOSE: root layout, Vietnamese and elderly-friendly. SIGNATURES: export const metadata: Metadata = {title 'Trợ lý Thủ tục Hành chính', description in Vietnamese}; export default function RootLayout({children}). HOW: <html lang='vi'><body> importing globals.css; system font stack; bg-slate-50 text-slate-900; generous base sizing (text-[17px] md:text-lg). NO global nav — pages own their headers so the /chat iframe embed stays chrome-free. HOW TO TEST: all pages render inside it."
  },
  {
   "path": "src/app/globals.css",
   "specification": "PURPOSE: Tailwind entry + accessibility base. CONTENT: @tailwind base/components/utilities; base layer — comfortable line-height, strong :focus-visible outline, minimum 44px tap targets on buttons/inputs; component classes .btn (large padding, rounded-lg, font-semibold), .card (white rounded-xl shadow-sm p-5), .badge-eco (amber pill labeled 'Chế độ tiết kiệm'). HOW TO TEST: visual — large type and visible focus rings on every interactive element."
  },
  {
   "path": "src/app/page.tsx",
   "specification": "PURPOSE: home — entry to the whole flow. HOW: static server component; hero 'Trợ lý AI hướng dẫn thủ tục hành chính' with a plain-Vietnamese subtitle; a search form (method GET, action /chat, input name q, placeholder 'Ví dụ: Tôi muốn đăng ký kết hôn'); two large cards linking /chat?procedure=MARRIAGE_REGISTRATION and /chat?procedure=BIRTH_REGISTRATION; footer links to /sources, /widget-demo, /admin; <SourceFooter showDisclaimer /> (no source props). HOW TO TEST: renders; links navigate."
  },
  {
   "path": "src/app/chat/page.tsx",
   "specification": "PURPOSE: guided-intake screen hosting ChatIntake. SIGNATURES: export default function ChatPage({searchParams}: {searchParams: {q?: string; procedure?: string; embed?: string}}). HOW: thin server component; embed = searchParams.embed === '1'; minimal '← Trang chủ' header hidden when embed; <ChatIntake initialQuery={q} initialProcedure={procedure} embed={embed} />. HOW TO TEST: /chat renders; /chat?embed=1 shows no site chrome."
  },
  {
   "path": "src/app/checklist/page.tsx",
   "specification": "PURPOSE: checklist & guidance screen. HOW: 'use client' page (useSearchParams inside <Suspense>); sessionId from query, {sessionId, token} from sessionStorage key 'psp_session'; missing or mismatched → friendly Vietnamese message + link to /chat (fresh tabs cannot recover the capability token by design); fetch GET /api/v1/guided-intake/{sessionId}/guidance with header X-Session-Token; loading and error states rendering the envelope message; <ChecklistView guidance />; primary button 'Điền biểu mẫu' → POST /api/v1/applications {sessionId, messageId: crypto.randomUUID()} with the token header → router.push('/form/' + applicationId); <SpeechButton text={plain-text checklist summary} />. HOW TO TEST: manual against integration §3/§4 behavior — conditional DIVORCE_DOCUMENT and the source/disclaimer footer visible.\n\nREVISION (reviewer feedback — MUST implement): Handle formAvailable=false from the guidance payload: no form CTA, guidance-only note shown (mirrors ChecklistView behavior)."
  },
  {
   "path": "src/app/form/[applicationId]/page.tsx",
   "specification": "PURPOSE: dynamic form screen pinned to the application's form version — fetches EVERYTHING via the authorized API (no direct Prisma; preserves the provider boundary). HOW: 'use client'; token from sessionStorage 'psp_session'; GET /api/v1/applications/{applicationId} with X-Session-Token; loading state; 403/404 → friendly restart message linking /chat; on success render heading (procedure name + 'Phiên bản ' + formVersion), <SourceFooter sourceUrl version lastCheckedAt /> from the response's procedure block, and <ApplicationFormRunner applicationId fields rules initialData={data} revision formVersion updateAvailable newVersion token />. HOW TO TEST: renders v1.0 fields purely from JSON before approval; update banner after approval; v2.0 fields after confirmed migration."
  },
  {
   "path": "src/app/result/page.tsx",
   "specification": "PURPOSE: pre-submission validation report — client page reusing the validate route so orchestration lives in exactly one place. HOW: 'use client' (useSearchParams in <Suspense>); applicationId from query, token from sessionStorage; GET the application with the token header, then POST /api/v1/forms/{formCode}/validate {formVersion, data, applicationId} with X-Session-Token (the applicationId + token satisfy version-access control even after the version retires); render <ValidationReport valid errors aiExplanation applicationId formVersion aiMode degraded />; success state 'Hồ sơ hợp lệ, sẵn sàng nộp' with <SourceFooter showDisclaimer />; fetch errors surface the envelope message. HOW TO TEST: canonical data saved to an application yields the 3-error report with the AI box and a back link to /form/{id}; still works after v2.0 activation (pinned-version access)."
  },
  {
   "path": "src/app/sources/page.tsx",
   "specification": "PURPOSE: transparency page — nguồn, phiên bản, ngày cập nhật. HOW: export const dynamic = 'force-dynamic'; async server component calling getProvider().getCatalogOverview() (provider boundary, NOT raw Prisma); per procedure: name, code, agency, source anchor rendered only when safeHttpsUrl passes (else plain text), active procedure version, form versions with status/effectiveFrom/effectiveTo, lastCheckedAt formatted vi-VN; prominent DISCLAIMER paragraph and a note that data was normalized from public sources. HOW TO TEST: lists both seeded procedures with real https source links; after approval shows v2.0 ACTIVE.\n\nREVISION (reviewer feedback — MUST implement): Also display each procedure version's legalBasisText alongside source_url/version/last_checked_at."
  },
  {
   "path": "src/app/admin/page.tsx",
   "specification": "PURPOSE: admin console shell — contains NO data and NO server-side queries; the entire admin surface sits behind the token-checked APIs. HOW: server component rendering a heading 'Quản trị — demo, bảo vệ bằng mã X-Admin-Token dùng chung' and <AdminConsole />. HOW TO TEST: the page itself leaks nothing; data appears only after a valid token is entered."
  },
  {
   "path": "src/app/widget-demo/page.tsx",
   "specification": "PURPOSE: fake portal page proving the embeddable widget. HOW: server component styled as 'Cổng Dịch vụ công (Mô phỏng)' with a visible banner 'Trang mô phỏng phục vụ demo — không phải website chính thức' and static portal-like blocks; loads the widget via next/script: <Script src='/widget.js' strategy='afterInteractive' data-api-key='demo-key' data-theme='light' data-position='bottom-right' />. HOW TO TEST: floating launcher appears bottom-right and the full search → clarify → checklist → form flow completes inside the panel."
  },
  {
   "path": "src/components/DynamicForm.tsx",
   "specification": "PURPOSE: the schema-only form renderer (ZERO hardcoded administrative fields in JSX) plus the form-page orchestrator with optimistic-concurrency and migration UX. SIGNATURES ('use client'): export default function DynamicForm({fields, rules, initialData, submitLabel, onSubmit}: {fields: FieldDef[]; rules?: RuleDef[]; initialData?: Record<string, unknown>; submitLabel: string; onSubmit(data: Record<string, unknown>, clientErrors: ValidationErrorItem[]): void}); export function ApplicationFormRunner({applicationId, fields, rules, initialData, revision, formVersion, updateAvailable, newVersion, token}). HOW (DynamicForm): state initialized so EVERY visible field id has a key ('' default) — pairs with fail-closed full-form validation; render by field.type switch: text/textarea/number/date native inputs (number stores Number, date stores YYYY-MM-DD), select from options, radio as large button group, checkbox boolean, file input storing only the file NAME string, province select over PROVINCES from '@/lib/constants'; visibleWhen re-evaluated via evaluateCondition on every change and keys of newly hidden fields deleted; large labels, required marked *, inline red per-field errors from clientErrors after a submit attempt; on submit clientErrors = rules ? runRules(fields, rules, data) : [] then onSubmit(visible data, clientErrors). HOW (ApplicationFormRunner): tracks revision in state; all fetches send X-Session-Token = token. Update banner when updateAvailable ('Có biểu mẫu phiên bản ' + newVersion + ' theo quy định mới' + button 'Xem thay đổi') → POST /api/v1/applications/{id}/migrate {} (preview) → panel listing migrated fields, dropped fields, and ONE RADIO GROUP per needsConfirmation entry (options labeled from the NEW field labels, NOTHING preselected — the user must choose) → 'Xác nhận cập nhật' → POST migrate {confirm: true, resolutions} → on 200 refetch the application (new fields, data, revision) and show 'Biểu mẫu đã được cập nhật theo quy định mới.'. Submit flow: show clientErrors inline AND always PUT /api/v1/applications/{id} {data, revision} → on 409 CONCURRENT_UPDATE refetch and show 'Dữ liệu đã thay đổi ở nơi khác, đã tải lại phiên bản mới nhất.' without navigating; on 200 update revision then router.push('/result?applicationId=' + id). Degraded/mock flags from responses surface the eco badge. HOW TO TEST: v1.0 and v2.0 render purely from JSON; divorce_document appears only when previously_married=true; residence resolution is never auto-selected; stale-revision path recovers with a refetch."
  },
  {
   "path": "src/components/ChatIntake.tsx",
   "specification": "PURPOSE: the guided-intake chat UI with voice. SIGNATURES ('use client'): export default function ChatIntake({initialQuery, initialProcedure, embed}). HOW: message list (bot/user bubbles, large text); phases search → intake → done. SEARCH: input + mic + send; POST /api/v1/procedures/search {message, province?}; on 200 render a procedure card (name, 'Độ tin cậy ' + Math.round(confidence*100) + '%', source link) + confirm button → POST /api/v1/guided-intake/start {procedureCode, presetAnswers: {province} only when known, intentMessage: message}; store {sessionId, token: accessToken} in sessionStorage 'psp_session'; on 404 show the two supported procedures as buttons; initialProcedure skips straight to start; initialQuery auto-sends once. INTAKE: render flow.next by fieldType — radio/select as big option buttons from options, province as PROVINCES select, text input; each answer POST /guided-intake/{sessionId}/answer {questionCode, value, messageId: crypto.randomUUID()} with X-Session-Token; progress 'Câu {answered+1}/{total}'; 'Sửa câu trả lời' re-opens any answered question (overwrite; when removedAnswers is non-empty, a bot note lists the cleared downstream answers). DONE: buttons 'Xem danh sách giấy tờ' → /checklist?sessionId=... and 'Điền biểu mẫu ngay' (POST /applications with messageId then navigate). VOICE (explicit action only): mic uses window.SpeechRecognition/webkitSpeechRecognition lang 'vi-VN' when available; else MediaRecorder capture (max 10s) POSTed multipart to /api/v1/speech/transcribe; bot messages offer <SpeechButton text />. TRUST UI: eco badge when aiMode === 'mock' or degraded; DISCLAIMER small print under the thread; 429 shows the retry message. EDGES: every fetch error renders the envelope message, never crashes. HOW TO TEST: full keyboard-only flow with AI_PROVIDER=mock; mic path manually in Chrome."
  },
  {
   "path": "src/components/ChecklistView.tsx",
   "specification": "PURPOSE: presentational guidance renderer with trust cues. SIGNATURES ('use client'): export default function ChecklistView({guidance}). HOW: header (procedure name, agency); document list — name, 'Bản chính: n · Bản sao: m', conditional items visually tagged with item.reason; vertical step timeline from guidance.steps; durationText + feesText row; <SourceFooter sourceUrl version lastCheckedAt showDisclaimer /> fed from guidance.procedure. HOW TO TEST: previously_married=true shows 4 marriage documents including DIVORCE_DOCUMENT with its reason, else 3.\n\nREVISION (reviewer feedback — MUST implement): (a) Render each step's 'example' line under its description in a muted style prefixed 'Ví dụ:'. (b) Render submissionType as a labeled badge per document: SUBMIT -> 'Nộp', PRESENT -> 'Xuất trình (không nộp)', SYSTEM_LOOKUP -> 'Cơ quan tự tra cứu CSDL dân cư — bạn không cần nộp'. (c) Show legalBasisText in the SourceFooter area ('Căn cứ pháp lý: ...'). (d) When the procedure has no active form (guidance-only), HIDE the fill-form CTA and show the note 'Thủ tục này hiện hỗ trợ tra cứu và hướng dẫn; biểu mẫu điện tử sẽ được bổ sung.'"
  },
  {
   "path": "src/components/ValidationReport.tsx",
   "specification": "PURPOSE: pre-submission report — deterministic errors and AI explanation, clearly separated. SIGNATURES ('use client'): export default function ValidationReport({valid, errors, aiExplanation, applicationId, formVersion, aiMode, degraded}). HOW: valid → green success card; else red summary 'Phát hiện {n} lỗi cần sửa trước khi nộp' + list rows: field id(s), message, 'Cách khắc phục: ' + suggestion; CONFLICT rows show both field ids; severity 'warning' rows styled amber. Below, a visually distinct 'Trợ lý AI giải thích' box with aiExplanation, labeled 'AI chỉ giải thích mã lỗi — nội dung pháp lý lấy từ cơ sở dữ liệu'; <SpeechButton text={plain text of errors + explanation} />; primary button 'Quay lại sửa biểu mẫu' → /form/{applicationId}; eco badge when aiMode === 'mock' || degraded. HOW TO TEST: canonical case renders exactly 3 rows in rule order."
  },
  {
   "path": "src/components/AdminConsole.tsx",
   "specification": "PURPOSE: the entire admin UI — token gate, overview tables, change-request diff review, approval. SIGNATURES ('use client'): export default function AdminConsole(). HOW: token input ('X-Admin-Token') persisted to sessionStorage 'psp_admin_token'; 'Tải dữ liệu' fetches GET /api/v1/admin/overview and GET /api/v1/admin/change-requests with the header; renders (1) procedures/form-versions table (code, version, status, effectiveFrom/To), (2) AI usage table per service (calls, tokens, audio seconds, avg latency, estimated cost, cache hits, degraded count), (3) CR cards: status pill, sourceUrl link, diff.summary, diff table (added green id/label, removed red, changed); PENDING cards get 'Phê duyệt & kích hoạt' → POST approve → show activatedVersion/closedVersion and refetch both lists. Error mapping: 401 'Sai mã quản trị', 409 ALREADY_PROCESSED 'Đã xử lý trước đó', 409 CONCURRENT_UPDATE 'Đang có thao tác khác, thử lại', 400 INVALID_TARGET_VERSION 'Phiên bản đích không hợp lệ', 429 'Quá nhiều lần thử, chờ ít phút'. Visible note: shared demo token, demo-only mechanism. HOW TO TEST: manual acceptance — versions table flips v2.0 to ACTIVE after approving here."
  },
  {
   "path": "src/components/SourceFooter.tsx",
   "specification": "PURPOSE: reusable trust block for every legal-content screen. SIGNATURES: export default function SourceFooter({sourceUrl, version, lastCheckedAt, showDisclaimer = true, eco = false}: {sourceUrl?: string; version?: string; lastCheckedAt?: string | Date; showDisclaimer?: boolean; eco?: boolean}) — server-compatible (no hooks). HOW: small print; 'Nguồn: ' rendered as an anchor (target _blank rel noopener) ONLY when an inline new URL(sourceUrl) parse yields protocol 'https:' (do not import server-only schema-guards), otherwise plain text; 'Phiên bản: {version} · Cập nhật: {dd/MM/yyyy}' when provided; the DISCLAIMER constant (imported from '@/lib/constants') when showDisclaimer; eco badge when eco. HOW TO TEST: visible on checklist/form/result/sources/home; javascript: URLs never render as links."
  },
  {
   "path": "src/components/SpeechButton.tsx",
   "specification": "PURPOSE: the single 'Nghe nội dung' TTS control (never automatic). SIGNATURES ('use client'): export default function SpeechButton({text, label = 'Nghe nội dung'}). HOW: onClick only — if window.speechSynthesis exists: SpeechSynthesisUtterance(text) with lang 'vi-VN', toggle-cancels any current utterance; else POST /api/v1/speech/synthesize {text: text.slice(0, 500)} → blob → new Audio(URL.createObjectURL(blob)).play(); states idle/loading/playing with aria-pressed and an inline SVG speaker icon; errors degrade to a disabled state with tooltip 'Thiết bị không hỗ trợ đọc'. HOW TO TEST: manual — reads Vietnamese aloud in Chrome; server path returns playable WAV under mock."
  },
  {
   "path": "public/widget.js",
   "specification": "PURPOSE: self-contained embeddable widget script (ES5-compatible, no build step, no dependencies). HOW: IIFE guarded by a window flag (loading twice is a no-op); script = document.currentScript; read data-api-key (decorative demo parameter passed through, NOT authentication), data-theme ('light'|'dark' default light), data-position ('bottom-right'|'bottom-left' default bottom-right); base = new URL(script.src).origin; inject scoped styles (all classes prefixed pspw-); fixed floating launcher (56px circle, z-index 999999, aria-label 'Mở trợ lý thủ tục hành chính'); click toggles a panel (380x640 max, full-screen on mobile) with header 'Trợ lý thủ tục hành chính' + close button and an iframe src = base + '/chat?embed=1&apiKey=' + encodeURIComponent(apiKey) — the iframe drives the whole flow against the same APIs, holding its capability token in its own sessionStorage; expose window.PSPWidget = {open, close}; no other globals. HOW TO TEST: /widget-demo shows the launcher; the flow completes inside the panel."
  },
  {
   "path": "tests/rule-engine.test.ts",
   "specification": "PURPOSE: authoritative lock of the validation contract, fail-closed semantics, strict dates, coercion, sanitization, ReDoS runtime downgrade, and the CANONICAL demo case. HOW: vitest importing from '@/lib/rule-engine' with inline fixtures (RuleDef fixtures may be hand-built to bypass parse-time guards where the runtime path is under test). REQUIRED CASES: isEmptyValue — false and 0 not empty; '', null, undefined, [] empty. coerceForComparison + evaluateCondition matrix — equals boolean vs 'true' string; not_equals; in with per-candidate coercion (['1', 2] matches numeric 2 and string '1'; NaN candidate never matches); greater_than with numeric string; NaN comparisons false; is_empty/not_empty; missing key behaves as undefined. isValidIsoDate — '2024-02-29' true; '2025-02-29', '2025-02-30', '2025-13-01', '2025-1-1', 'garbage' false. FULL-FORM FAIL-CLOSED — required fires MISSING_REQUIRED when the key is ABSENT from data; required passes for false and 0; hidden field (visibleWhen false) skipped for required and regex; a rule referencing a field id not in the schema is skipped; cross_field_conflict and conditional_document evaluate regardless of visibility. REGEX — '123' vs ^[0-9]{12}$ INVALID_FORMAT; 12 digits pass; empty skipped; 501-char value INVALID_FORMAT immediately; the adversarial pattern (a+)+$ injected as a runtime RuleDef emits RULE_CONFIG_INVALID severity 'warning' WITHOUT executing (and completes instantly against a long 'aaaa...b' value — the ReDoS guard test); a 201-char pattern and a non-compiling pattern likewise downgrade. DATES — tomorrow (from todayInVietnam) DATE_IN_FUTURE; yesterday passes; '2025-02-30' INVALID_DATE; date_after fires DATE_ORDER_INVALID on end <= start, INVALID_DATE on a malformed side, skipped when a side is empty. RANGE — 0 and 11 vs {min 1, max 10} OUT_OF_RANGE; 2 passes; non-numeric skipped. conditional_required — condition true + empty MISSING_REQUIRED, condition false passes. cross_field_conflict — {previously_married: false, marriage_number: 2} fires CONFLICT with fields ['previously_married','marriage_number']; {true, 2} passes. conditional_document — condition true + empty MISSING_DOCUMENT; filename passes; condition false passes. Every emitted item has non-empty Vietnamese message and suggestion. sanitizeFormData — unknown key UNKNOWN_FIELD; nested object INVALID_VALUE_TYPE; numeric string → number; 'true' → boolean for checkbox/boolean radio; 2001-char string VALUE_TOO_LONG; non-object data not ok; ok returns a NEW sanitized copy (input not mutated). CANONICAL CASE (defined here ONCE, referenced by integration §4): fixture mirroring seeded marriage v1.0 fields+rules; input EXACTLY {male_full_name: 'Nguyễn Văn A', male_birth_date: '1995-01-15', male_identity_number: '123', female_full_name: 'Trần Thị B', female_birth_date: '', female_identity_number: '001199001234', residence: '12 Phố Huế, Hà Nội', province: 'Hà Nội', previously_married: false, marriage_number: 2, submission_channel: 'online'}; expect errors.length === 3 AND errors.map of {code, field, fields} deep-equals [{code 'MISSING_REQUIRED', field 'female_birth_date', fields undefined}, {code 'INVALID_FORMAT', field 'male_identity_number', fields undefined}, {code 'CONFLICT', field undefined, fields ['previously_married','marriage_number']}] in that order."
  },
  {
   "path": "tests/form-versioning.test.ts",
   "specification": "PURPOSE: lock temporal version selection, historical resolvability, timezone boundaries, the approval transition, and target-validity rules. HOW: vitest importing from '@/lib/form-migration' with plain VersionLike fixtures. REQUIRED CASES: v1 ACTIVE (effectiveFrom 2026-01-01T00:00:00+07:00, effectiveTo null) + v2 DRAFT → select at 2026-07-01 returns v1 (DRAFT never selected); before effectiveFrom → null; a version with effectiveFrom null is never selected regardless of status. TIMEZONE BOUNDARY — at new Date('2025-12-31T16:59:59Z') → null; at new Date('2025-12-31T17:00:00Z') (Vietnam midnight) → v1. activateVersion(versions, '2.0', T) → target ACTIVE effectiveFrom T effectiveTo null; v1 RETIRED effectiveTo T; changed contains both; closed is v1; inputs not mutated. After activation, select at T and later → v2 (at === old effectiveTo excludes old — exclusive end; inclusive start includes new). HISTORICAL — select at T minus 1ms still returns the now-RETIRED v1 (retired versions remain resolvable inside their closed interval). TARGET VALIDITY — unknown target throws FORM_VERSION_NOT_FOUND; target with status ACTIVE or RETIRED throws INVALID_TARGET_VERSION; a DRAFT '0.9' older than active '1.0' throws INVALID_TARGET_VERSION. compareVersions('2.0','1.0') > 0 and compareVersions('1.10','1.9') > 0."
  },
  {
   "path": "tests/migration.test.ts",
   "specification": "PURPOSE: lock migration semantics — copy compatible, never guess, user resolutions honored, preview purity. HOW: vitest importing computeMigration; fixtures: v1 fields [male_full_name text, residence text, marriage_number number], v2 fields [male_full_name, permanent_address, temporary_address, phone_number, marriage_number], hints [{from 'residence', candidates ['permanent_address','temporary_address']}]. REQUIRED CASES: data {male_full_name 'Nguyễn Văn A', residence '12 Phố Huế, Hà Nội', marriage_number 1} without resolutions → migrated contains male_full_name and marriage_number with values copied; migratedData has NO residence/permanent_address/temporary_address key; needsConfirmation deep-equals [{from 'residence', value '12 Phố Huế, Hà Nội', options ['permanent_address','temporary_address']}]. WITH resolutions {residence: 'permanent_address'} → migratedData.permanent_address === the old value; applied deep-equals [{from 'residence', to 'permanent_address'}]; needsConfirmation empty. resolutions {residence: 'phone_number'} (not a candidate) throws INVALID_RESOLUTION. Empty residence '' → neither confirmation nor dropped. An old-only valued field with NO hint → in dropped and absent from migratedData. A hint whose candidates all miss the new schema → falls to dropped. phone_number (new-only) absent from migratedData. The input data object is not mutated (preview purity)."
  },
  {
   "path": "tests/intake-machine.test.ts",
   "specification": "PURPOSE: lock the interview state machine, pruning, answer validation, guidance reasons, and the exact disclaimer. HOW: vitest importing from '@/lib/intake-machine' and DISCLAIMER from '@/lib/constants', with QuestionRow fixtures mirroring the seeded marriage questions. REQUIRED CASES: computeQuestionFlow orders by orderNumber and returns the first unanswered applicable question; a question with a condition is excluded until the condition holds; preset answers reduce total and next (province preset skips its question); answered/total count only applicable questions. pruneAnswers — unknown keys removed; an answer whose question became inapplicable after a correction is removed; FIXPOINT: a chain of dependent questions is fully pruned; returns the removed list. validateAnswer — radio 'true' string coerces to boolean true against boolean options; a value matching no option throws INVALID_ANSWER with status 400; empty text throws; over-length text throws; valid text and a PROVINCES member pass through; a non-member province throws. buildGuidance — DIVORCE_DOCUMENT included with reason EXACTLY 'Áp dụng vì bạn đã từng đăng ký kết hôn' when previously_married=true and excluded when false; unconditional documents always present in orderNumber order; steps/durationText/feesText passed through; guidance.disclaimer === DISCLAIMER by strict string equality."
  },
  {
   "path": "tests/infra.test.ts",
   "specification": "PURPOSE: lock the infrastructure contracts — cache, idempotency keys, rate limiting and proxy trust, auth/token security, config policy, schema guards incl. ReDoS, upstream bounds, TTS keys, intent. HOW: vitest, DB-free; vi.useFakeTimers where windows matter; Request objects crafted with forged headers; process.env toggled per test and restored (config getters read env per call); _reset*ForTests between cases; vi.stubGlobal('fetch') for upstream. REQUIRED SECTIONS: CACHE — set/get round-trip; TTL expiry; del; 500-entry eviction; cachedJson hit flag. IDEMPOTENCY KEYS — canonicalJson stable under object key order; buildIdempotencyKey null without messageId; identical parts identical key; changing operation, resourceId, or body changes the key. RATE LIMIT — limit N allows N and the N+1th throws 429 with retryAfterSeconds; separate buckets independent; window reset; TRUST_PROXY unset/'0' (DEFAULT OFF) → two requests with different x-forwarded-for values share ONE bucket (forged headers ignored, fail-closed); TRUST_PROXY '1' → LAST x-forwarded-for entry identifies the client, two platform-appended IPs tracked separately, client-prepended spoof entries ignored; check/consume pair only counts failures; RATE_LIMIT_PER_MINUTE 'abc' and '-5' → default 10. AUTH — generateAccessToken returns 43-char base64url, two calls differ; hashToken deterministic and ≠ input; verifyToken true/false and false on malformed input without throwing; requireSessionToken 403 on missing/empty/oversized/wrong header. CONFIG — prod + ADMIN_TOKEN missing/short/'demo-admin-token' throws; prod + http OPENAI_BASE_URL throws; dev + http://localhost passes; credentialed URL throws; trailing slash stripped. SCHEMA GUARDS — valid marriage fixtures pass; duplicate field id, unknown type, bad operator, missing options for select, non-compiling pattern, 201-char pattern, and (a+)+$ EACH throw DATA_INTEGRITY; safeHttpsUrl accepts https://dichvucong.gov.vn/x, rejects http:, javascript:, non-strings. UPSTREAM — stubbed fetch: non-2xx, wrong content-type, oversized Content-Length, and unparseable JSON each throw UpstreamError; happy path returns parsed JSON. TTS KEY — makeTtsCacheKey('a|b','c',1,'vi') !== makeTtsCacheKey('a','b|c',1,'vi'); identical tuples equal. INTENT — 'Tôi muốn đăng ký kết hôn' → MARRIAGE_REGISTRATION 0.95; folded 'toi muon dang ky ket hon' matches; 'khai sinh' → BIRTH_REGISTRATION; longest keyword wins; '' → null. CONSTANTS — PROVINCES has exactly 34 entries."
  },
  {
   "path": "tests/integration/flow.test.ts",
   "specification": "PURPOSE: executable end-to-end contract over the REAL route handlers and the REAL database — every guarantee in the traceability matrix that needs persistence lives here. HOW: vitest; the suite FAILS when unconfigured: at module top, if process.env.SKIP_INTEGRATION === '1' use describe.skip with a console warning; otherwise beforeAll THROWS a clear error when DATABASE_URL is unset (CI cannot silently skip). beforeAll: set process.env.TRUST_PROXY='1' (explicit opt-in so forged x-forwarded-for identities work; one case later proves '0' ignores them), AI_PROVIDER='mock', ADMIN_TOKEN a fixed test value; import and run seed main() TWICE asserting stable row counts and marriage form versions exactly {1.0, 2.0}. Route handlers are imported directly from their route.ts files and invoked as plain functions with constructed Request objects and params; each logical section forges a DISTINCT x-forwarded-for so rate-limit state never bleeds. REQUIRED SECTIONS in order: §1 SEARCH — {message 'Tôi muốn đăng ký kết hôn', province 'Hà Nội'} → 200 MARRIAGE_REGISTRATION, confidence ≥ 0.9, matchedBy 'keyword'. §2 SESSION + TOKEN + IDEMPOTENCY — start with presetAnswers {province 'Hà Nội'} → 201 with accessToken; the DB Session row stores hashToken(token) and NOT the plaintext; progress.total excludes the preset question; start with presetAnswers {province 'Nowhere'} → 400 INVALID_ANSWER and with {bogus: 1} → 400 UNKNOWN_QUESTION; answer with a wrong token → 403; answers has_foreign_element=false, previously_married=true, submission_channel='online' → done true; repeating the last answer with the SAME messageId returns the identical body plus X-Idempotent-Replay and does not change the session (row counts/updatedAt prove single execution); firing the same messageId TWICE CONCURRENTLY (Promise.all) yields two identical bodies and one execution. §3 GUIDANCE — checklist contains DIVORCE_DOCUMENT with the exact reason; disclaimer strictly equals DISCLAIMER; second call meta.cached true. §4 APPLICATION + VALIDATE ACCESS — POST applications with messageId twice → 201 then replay, exactly ONE row; formVersion '1.0' with province and previously_married prefilled; GET with wrong token 403; PUT {data {bogus_key 'x'}, revision 0} → 400 INVALID_FORM_DATA naming bogus_key; PUT the CANONICAL payload (as defined in tests/rule-engine.test.ts) with revision 0 → 200 revision 1; PUT again with STALE revision 0 → 409 CONCURRENT_UPDATE; anonymous validate {formVersion '1.0', data canonical} → 200 valid false with EXACTLY the 3 ordered canonical errors, non-empty Vietnamese aiExplanation, aiMode 'mock'; anonymous validate {formVersion '2.0'} (DRAFT) → 403 VERSION_NOT_ACCESSIBLE. §5 ADMIN + APPROVAL — GET active → '1.0'; approve without token → 401; five bad tokens from one fresh IP → 429; approve with ADMIN_TOKEN (different IP) → 200 activatedVersion '2.0' closedVersion '1.0'; GET active → '2.0' including permanent_address; second approve → 409 ALREADY_PROCESSED; MULTI-CR RACE: reset v2.0→DRAFT/v1.0→ACTIVE via prisma, create a SECOND PENDING CR for the same form targeting '2.0', Promise.all both approves → exactly one 200, the other 409/400, and exactly ONE ACTIVE version remains; TARGET VALIDITY: a CR targeting the now-RETIRED '1.0' → 400 INVALID_TARGET_VERSION; POST-RETIREMENT ACCESS: anonymous validate '1.0' → 403, but with the §4 applicationId + token (pinned 1.0) → 200. §6 MIGRATION — preview lists needsConfirmation [{from 'residence', options ['permanent_address','temporary_address']}] and a re-GET shows the application UNCHANGED (still 1.0, residence intact); confirm without resolutions → 400 MISSING_RESOLUTION; a PUT (new residence value, current revision) BETWEEN preview and commit; confirm with resolutions {residence 'permanent_address'} → 200 committed and permanent_address equals the POST-preview value (in-transaction re-read preserved the concurrent save); GET shows formVersion '2.0'; migrate again → 409 NO_NEWER_VERSION; bogus resolution → 400 INVALID_RESOLUTION. §7 TRANSCRIBE — request with Content-Length header 7000000 → 413 AUDIO_TOO_LARGE; PNG magic bytes → 400 UNSUPPORTED_AUDIO_TYPE; truncated RIFF garbage → 400; a small valid PCM WAV generated in-test → 200 with the canned transcript, audioSeconds ≈ samples/rate; afterwards ONLY an AiUsageLog stt row was added (numeric/flag fields only) and no table stores the transcript. §8 SYNTHESIZE — invalid voice → 400; {text 'Xin chào'} twice → X-Cache-Hit false then true with byte-identical bodies; the mock WAV passes inspectAudio; ai_usage_logs contains a tts cacheHit=true row; admin overview usage reflects llm/stt/tts. §9 RATE LIMIT — with RATE_LIMIT_PER_MINUTE='5' and a fresh IP, six rapid search calls → the 6th is 429 with Retry-After and the envelope; with TRUST_PROXY='0', two different forged IPs share the global bucket; env restored afterward. §10 IDEMPOTENCY CRASH RECOVERY — call withIdempotency directly with a key and an exec that creates a Session row via tx then THROWS: assert zero new Session rows AND zero IdempotencyRecord rows (both rolled back); retry the same key with a succeeding exec → executes exactly once and a further call replays with replayed=true."
  },
  {
   "path": "README.md",
   "specification": "PURPOSE: the judge-facing front door (Vietnamese). CONTENT, in order: project name + one-line pitch; LIVE URL placeholder ('https://<service>.onrender.com' with a bold reminder to paste the real URL after deploy); 3-minute demo script (numbered happy path: home -> /chat 'Tôi muốn đăng ký kết hôn' (or voice) -> clarifying questions -> checklist (point out legal basis + SYSTEM_LOOKUP badge) -> form -> /result with the 3 canonical errors -> /admin approve v2.0 -> form shows update banner -> /widget-demo); QUICKSTART: npm install; docker compose up -d db; npx prisma db push; npm run seed; npm run dev; ENV table from .env.example incl. the demo ADMIN_TOKEN value and AI_PROVIDER=mock|openai semantics; DEPLOYMENT: Render blueprint (render.yaml), set OPENAI_API_KEY + ADMIN_TOKEN in dashboard, NOTE the free-plan spin-down (~15 min idle -> 40-60s cold start): either the $7 starter instance for demo week or an UptimeRobot ping of '/' every 5 minutes, and ALWAYS warm the URL right before the judging slot; links to docs/ARCHITECTURE.md, docs/ONE_PAGER.md, openapi.yaml and the /architecture page; the standard disclaimer that demo data is normalized from public sources and NO live government connection exists. HOW TO TEST: n/a (documentation)."
  },
  {
   "path": "docker-compose.yml",
   "specification": "PURPOSE: local dev dependencies only. Service 'db': postgres:16-alpine, port 5432, POSTGRES_PASSWORD/USER/DB matching .env.example DATABASE_URL, named volume, healthcheck pg_isready. Service 'redis': redis:7-alpine under profile 'redis' (optional — the app defaults to in-memory drivers). No app service — the app runs via npm run dev. HOW TO TEST: docker compose up -d db && pg_isready against localhost:5432 succeeds."
  },
  {
   "path": "docs/ARCHITECTURE.md",
   "specification": "PURPOSE: the required architecture documentation ('system diagram and details on models and APIs used'). CONTENT: (1) a Mermaid flowchart: Citizen/Widget -> Next.js App Router pages -> /api/v1 route handlers -> {intent matcher -> LLM fallback} / {intake state machine} / {rule engine} -> Prisma -> PostgreSQL; a second lane showing the three AI seams (llm.ts, stt.ts, tts.ts) each with 'OpenAI-compatible upstream' and 'deterministic mock fallback (degraded=true)'; cache/rate-limit/idempotency as middleware boxes. (2) a models table: gpt-4o-mini (intent fallback + error explanation, JSON-constrained), whisper-1 (STT), tts-1 (TTS, voice map vi-female->nova/vi-male->onyx), all via OPENAI_BASE_URL-compatible endpoint, AI_PROVIDER=mock for keyless demo. (3) grounding & safety: LLM may only pick procedureCode from the DB catalog, confidence floor 0.6, validation never uses the LLM, LLM sees only {code, field id}. (4) API surface: the 13 /api/v1 endpoints one line each + link ../openapi.yaml. Written in Vietnamese with English identifiers. HOW TO TEST: mermaid block renders on GitHub; every endpoint listed exists in code."
  },
  {
   "path": "docs/ONE_PAGER.md",
   "specification": "PURPOSE: the required one-page summary (print-to-PDF friendly, Vietnamese). SECTIONS: VẤN ĐỀ (citizens lost in procedures, errors found only after submission, elderly/low-tech excluded); GIẢI PHÁP (grounded 3-stage AI assistant: intent -> DB-driven intake -> rule-engine validation; AI never decides legal facts); ĐỐI TƯỢNG (người dân lần đầu làm thủ tục, người lớn tuổi, cán bộ một cửa, đơn vị vận hành cổng DVC); LỘ TRÌNH TRIỂN KHAI 3 giai đoạn: (1) Pilot 1 phường/xã với nhóm thủ tục hộ tịch (kết hôn, khai sinh) — đo tỉ lệ hồ sơ đạt ngay lần đầu; (2) Tích hợp cổng DVC tỉnh qua widget nhúng + đồng bộ danh mục thủ tục/biểu mẫu từ dichvucong.gov.vn qua GovernmentApiProvider; (3) Mở rộng theo lĩnh vực (cư trú, căn cước, xuất nhập cảnh) + đăng nhập SSO VNeID + chuyển hồ sơ phức tạp cho cán bộ. Close with the mock-data disclaimer. Keep under one printed page. HOW TO TEST: n/a (documentation)."
  },
  {
   "path": "src/app/architecture/page.tsx",
   "specification": "PURPOSE: a judge-clickable /architecture page mirroring docs/ARCHITECTURE.md. Server component, static Vietnamese content, NO external libraries: the system diagram is built from Tailwind-styled flex/grid boxes and arrow glyphs (three lanes: user flow, AI seams with mock-fallback note, data layer), followed by the models table and the 13-endpoint list with a link to /api/openapi. Reuse the app shell/typography. HOW TO TEST: page renders server-side with no client JS errors; all listed routes respond."
  }
 ]
}
```
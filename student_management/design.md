```json
{
  "project_name": "student-profile-system",
  "description": "GOAL: A modern full-stack Student Profile Management System — Next.js 14 App Router + React 18 (TypeScript strict), TailwindCSS v3, and Prisma ORM over a local SQLite file — that lists students on a dashboard and supports create/edit/delete of the five fields (Student ID, Full Name, Email, Major, GPA), plus client-side search and summary metrics, behind a dark glassmorphism, responsive UI. CONTEXT: this application already exists complete in the repository; this plan faithfully specifies every source file so a coder reproduces/verifies it by translation. It is a verify-and-reproduce task, NOT greenfield invention — do not add scope beyond the request. CONSTRAINTS: (1) No .env — the Prisma datasource url is the hardcoded literal 'file:./dev.db'; never create, read, parse, or echo any .env or secret. (2) Single validation source of truth: src/lib/validation.ts is imported by StudentForm and both API routes; its exact English messages are asserted verbatim in validation.test.ts and MUST NOT change. (3) The HTTP status contract is locked by route.test.ts against a vi.mock'd Prisma client (no DB). (4) No authentication, authorization, or pagination — deliberate for a single-user local tool; findMany is intentionally unbounded because the full dataset is needed client-side for summary metrics + search. Do NOT introduce auth, CSRF/DNS-rebinding guards, rate limits, body-size caps, loopback binding, 413/415 handling, or control-character filtering — none exist, and adding them would contradict the locked contract and the route tests (which intentionally do not assert 401/403). (5) ASCII-pragmatic string semantics: length caps use String.length (UTF-16 code units, not graphemes); search uses String.prototype.toLowerCase (no locale/Unicode casefold). (6) TypeScript strict; the @/* -> ./src/* path alias is mirrored in tsconfig.json AND vitest.config.ts (required so vi.mock('@/lib/prisma') intercepts the same specifier). (7) The client Student type (id:number; studentId/fullName/email/major:string; gpa:number; createdAt/updatedAt: ISO string) is declared locally in each component that needs it (no shared model module); page.tsx converts Prisma Date -> toISOString() at the server/client boundary. (8) Do NOT author next-env.d.ts (Next-generated) or hand-write SQL migrations. TEST STRATEGY: Vitest across two environments — validation.test.ts (node) locks every rule/message; route.test.ts (node) locks the full status contract via a mocked Prisma client; StudentForm.dom.test.tsx and DeleteDialog.dom.test.tsx (jsdom) lock the hand-written modal a11y (focus trap wrapping both ends, Escape/overlay close, focus restore to trigger, validate-on-blur, clear-on-input, submit blocked while invalid). Remaining presentational styling is verified at compile time by build + lint. DONE-WHEN: `npm run test` passes all three surfaces; `npm run build` (prisma generate && next build) succeeds; `npm run lint` passes; and behaviorally — CRUD works against SQLite, validation messages are exact, POST/PATCH/DELETE status codes match the contract, invalid ids abc/0/-1/0x1/1e1/1.0 all resolve to 404 without touching the DB, a duplicate studentId yields 409, and summary metrics reflect the full dataset while search filters only the visible grid. Do not claim any command passed without actually running it. Assumptions (introduced here, separable from the user's request): (a) reproduce/verify the existing implementation faithfully with no new features; (b) DB setup canonicalizes on `prisma db push` — the committed prisma/migrations folder is out of scope for this plan and not authored here; (c) the build-time Google Fonts fetch (Inter/Outfit) needs network access and may fail offline — accepted; (d) no hardening/auth/pagination scope is added.",
  "files": [
    {
      "path": "package.json",
      "specification": "PURPOSE: npm manifest for the app. HOW TO IMPLEMENT: a private ESM package named 'student-profile-system' with \"type\":\"module\". scripts: dev='next dev'; build='prisma generate && next build'; start='next start'; lint='next lint'; test='vitest run'; db:push='prisma db push'; db:seed='tsx prisma/seed.ts'. dependencies: next ^14, react ^18, react-dom ^18, @prisma/client ^5, lucide-react ^0.400. devDependencies: prisma ^5, typescript ^5, @types/react ^18, @types/react-dom ^18, @types/node ^20, tailwindcss ^3, postcss ^8, autoprefixer ^10, eslint ^8, eslint-config-next ^14, vitest ^1, tsx ^4, jsdom ^24, @testing-library/react ^16, @testing-library/user-event ^14, @testing-library/jest-dom ^6. EDGE CASES: build runs prisma generate first so the client exists before next build. HOW TO TEST: every script name referenced by README.md resolves here; `npm run test|build|lint` invoke the listed tools."
    },
    {
      "path": "tsconfig.json",
      "specification": "PURPOSE: strict TypeScript config for Next.js. HOW TO IMPLEMENT: compilerOptions: target 'ES2020'; lib ['dom','dom.iterable','esnext']; module 'esnext'; moduleResolution 'bundler'; jsx 'preserve'; strict true; esModuleInterop true; allowJs true; skipLibCheck true; noEmit true; incremental true; resolveJsonModule true; isolatedModules true; allowSyntheticDefaultImports true; forceConsistentCasingInFileNames true; plugins [{ name:'next' }]; paths { '@/*': ['./src/*'] }. include ['next-env.d.ts','**/*.ts','**/*.tsx','.next/types/**/*.ts']; exclude ['node_modules']. EDGE CASES: the '@/*'->'./src/*' alias MUST match vitest.config.ts so vi.mock('@/lib/prisma') intercepts. HOW TO TEST: `next build` type-checks under strict mode with no errors."
    },
    {
      "path": "next.config.mjs",
      "specification": "PURPOSE: minimal Next.js configuration. HOW TO IMPLEMENT: default-export a single empty NextConfig object (const nextConfig = {}; export default nextConfig) with a JSDoc @type {import('next').NextConfig}. App Router is the Next 14 default and no experimental flags are needed. HOW TO TEST: `next build` loads it without error."
    },
    {
      "path": "postcss.config.mjs",
      "specification": "PURPOSE: PostCSS pipeline for Tailwind. HOW IMPLEMENT: default-export a config object with plugins { tailwindcss: {}, autoprefixer: {} } — tailwindcss processes the @tailwind directives in globals.css, autoprefixer adds vendor prefixes. HOW TO TEST: `next build` compiles CSS with utility classes present."
    },
    {
      "path": "tailwind.config.ts",
      "specification": "PURPOSE: Tailwind v3 design tokens. HOW IMPLEMENT: import type Config from 'tailwindcss' and colors from 'tailwindcss/colors'. content: ['./src/**/*.{ts,tsx}'] (covers every class source so none is purged). theme.extend.colors.brand: a navy ramp 50:#f2f5fb,100:#e2e8f4,200:#c6d2e8,300:#9db1d6,400:#6d89bf,500:#4a68a6,600:#39518a,700:#2f4270,800:#2a385e,900:#26314f,950:#141a2e; also emerald: colors.emerald and amber: colors.amber. fontFamily.sans ['var(--font-inter)','system-ui','sans-serif']; fontFamily.display ['var(--font-outfit)','var(--font-inter)','sans-serif']. keyframes 'fade-in-up': 0% {opacity:'0',transform:'translateY(8px)'} -> 100% {opacity:'1',transform:'translateY(0)'}. animation 'fade-in-up': 'fade-in-up 0.4s ease-out both'. plugins []. EDGE CASES: the content glob must include src so brand-*/fade-in-up classes survive purge. HOW TO TEST: build emits the brand palette and animation used by the components."
    },
    {
      "path": "vitest.config.ts",
      "specification": "PURPOSE: Vitest config for three test surfaces across two environments. HOW IMPLEMENT: defineConfig with test: { globals:true; environment:'node'; environmentMatchGlobs:[['src/**/*.dom.test.tsx','jsdom']]; setupFiles:['./src/test/setup.ts']; include:['src/**/*.test.ts','src/**/*.test.tsx'] } and resolve.alias { '@': new URL('./src', import.meta.url).pathname }. EDGE CASES: node is the default (fast, no DOM); only *.dom.test.tsx route to jsdom; the '@' alias MUST equal the tsconfig mapping so vi.mock('@/lib/prisma') intercepts the exact specifier the handlers import. HOW TO TEST: `vitest run` discovers all validation/route/component suites and runs the jsdom ones with a DOM."
    },
    {
      "path": ".gitignore",
      "specification": "PURPOSE: keep generated/local artifacts out of version control. HOW IMPLEMENT: ignore /node_modules; /.next and /out; the local SQLite files /prisma/dev.db and /prisma/dev.db-journal; *.log; .DS_Store; and .env* defensively (the app hardcodes its Prisma url and creates no .env). HOW TO TEST: node_modules and prisma/dev.db are absent from tracked files."
    },
    {
      "path": "prisma/schema.prisma",
      "specification": "PURPOSE: Prisma schema over local SQLite. HOW IMPLEMENT: generator client { provider = 'prisma-client-js' }. datasource db { provider = 'sqlite'; url = 'file:./dev.db' } — a hardcoded LITERAL, NOT env(), so no .env is needed. model Student { id Int @id @default(autoincrement()); studentId String @unique; fullName String; email String; major String; gpa Float; createdAt DateTime @default(now()); updatedAt DateTime @updatedAt }. EDGE CASES: the @unique on studentId is the DB guarantee behind the API's 409 duplicate contract; email is intentionally NOT unique (students may share a contact). `npx prisma db push` creates prisma/dev.db with this table and the unique index. HOW TO TEST: `prisma generate` succeeds; `prisma db push` yields a Student table with a unique index on studentId."
    },
    {
      "path": "prisma/seed.ts",
      "specification": "PURPOSE: idempotent sample-data seed. HOW IMPLEMENT: import PrismaClient, instantiate one client. Define an array of six students spanning majors and the full 0.0-4.0 GPA range so the emerald/amber/rose thresholds and summary metrics are all exercised: S1001 Ada Lovelace / Computer Science / 3.95; S1002 Grace Hopper / Mathematics / 3.6; S1003 Marie Curie / Physics / 2.85; S1004 Charles Darwin / Biology / 3.1; S1005 John Nash / Economics / 1.9; S1006 Nikola Tesla / Electrical Engineering / 0.75 (emails like ada.lovelace@example.edu). async main(): for each student prisma.student.upsert({ where:{studentId}, update:student, create:student }) (idempotent on the unique studentId), then console.log the count. Run main().catch(log + process.exit(1)).finally($disconnect). EDGE CASES: re-running refreshes rows rather than duplicating. HOW TO TEST: run via `npm run db:seed` after db push; a second run does not create duplicates."
    },
    {
      "path": "src/lib/prisma.ts",
      "specification": "PURPOSE: shared PrismaClient singleton safe across dev HMR. HOW IMPLEMENT: import PrismaClient. Widen globalThis via a narrow cast to { prismaGlobal?: PrismaClient }. Export a NAMED const prisma = globalForPrisma.prismaGlobal ?? new PrismaClient(). When process.env.NODE_ENV !== 'production', assign globalForPrisma.prismaGlobal = prisma so hot reloads reuse the instance instead of opening new connection pools. EDGE CASES: production processes get a fresh client and never touch the global. The NAMED export is required so vi.mock('@/lib/prisma') can substitute it in tests. HOW TO TEST: route.test.ts mocks this module; app code imports { prisma } from '@/lib/prisma'."
    },
    {
      "path": "src/lib/validation.ts",
      "specification": "PURPOSE: the single validation source of truth shared by the form and both API routes; no function throws; every message is stable English asserted verbatim by validation.test.ts. SIGNATURES/TYPES: export type StudentInput = { studentId:string; fullName:string; email:string; major:string; gpa:string|number }; export type FieldName = keyof StudentInput; export type FieldErrors = Partial<Record<FieldName,string>>; export const COMMON_MAJORS: string[] (Computer Science, Mathematics, Physics, Biology, Business Administration, Economics, Psychology, Mechanical Engineering, Electrical Engineering, English Literature); export functions validateStudentId(v:string):string|null, validateFullName(v:string):string|null, validateEmail(v:string):string|null, validateMajor(v:string):string|null, parseGpa(v:string|number):number|null, validateGpa(v:string|number):string|null, validateStudent(input:StudentInput):FieldErrors. CONSTANTS: STUDENT_ID_MAX=32, FULL_NAME_MAX=100, MAJOR_MAX=80; EMAIL_PATTERN = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/; PLAIN_DECIMAL_PATTERN = /^-?\\d+(\\.\\d+)?$/; GPA_MIN=0, GPA_MAX=4. HOW IMPLEMENT + EXACT MESSAGES: validateStudentId trims; empty -> 'Student ID is required'; length>32 -> 'Student ID must be 32 characters or fewer'; else null. validateFullName trims; empty -> 'Full name is required'; >100 -> 'Full name must be 100 characters or fewer'; else null. validateEmail trims; empty -> 'Email is required'; !EMAIL_PATTERN.test -> 'Enter a valid email address'; else null. validateMajor trims; empty -> 'Major is required'; >80 -> 'Major must be 80 characters or fewer'; else null. parseGpa: if typeof v==='number' n=v; else trim v, empty -> null, if !PLAIN_DECIMAL_PATTERN.test(trimmed) return null (rejecting '0x3','0b11','1e0','abc' BEFORE any Number() coercion), else n=Number(trimmed); finally if !Number.isFinite(n) return null (catches NaN/±Infinity) else n. validateGpa: detect empty first independent of parsing — if typeof v==='string' and v.trim() empty -> 'GPA is required'; else if typeof v==='number' and Number.isNaN(v) -> 'GPA is required'; then n=parseGpa(v); if n===null -> 'GPA must be a plain decimal number (e.g. 3.75)'; if n<GPA_MIN||n>GPA_MAX -> 'GPA must be between 0.0 and 4.0'; else null. validateStudent runs each validator and includes only fields whose validator returned non-null. EDGE CASES: length caps use String.length (UTF-16 code units, ASCII-pragmatic, not graphemes); the distinct 'required' vs 'plain decimal' vs 'range' GPA messages guarantee a user who typed something is never told the field is empty. HOW TO TEST: see validation.test.ts."
    },
    {
      "path": "src/lib/validation.test.ts",
      "specification": "PURPOSE: node Vitest suite locking every rule and EXACT message. HOW IMPLEMENT: import the validators/parseGpa/validateStudent and StudentInput; RE-DECLARE the message constants locally (not imported) so any source change must be deliberately mirrored here. HOW TO TEST — lock: validateStudentId valid ('S1001'); required for '' and '   '; length for 'a'.repeat(33); boundary null for 'a'.repeat(32). validateFullName valid; required for ''; length for 101 chars; boundary null for 100. validateEmail valid ('a@b.co'); required for ''; invalid for 'nope','a@b','a b@c.com','a@@b.com'. validateMajor valid; required for ''; length for 81 chars; boundary null for 80. parseGpa number branch: 3.5->3.5, 0->0, and NaN/Infinity/-Infinity->null. parseGpa string branch: '3.75'->3.75, '0'->0, ''/'   '->null, 'abc'/'0x3'/'0b11'/'1e0'->null. validateGpa: '0'/'4'/'2.7' null; ''/'   '->required; '-0.1'/'4.1'->range; for 'x','0x3','1e0' the INVALID message AND assert it is NOT the required message. validateStudent: {} for a fully valid input; for {studentId:'',fullName:'Ada Lovelace',email:'nope',major:'Physics',gpa:'5'} returns exactly {studentId:required,email:invalid,gpa:range}."
    },
    {
      "path": "src/app/layout.tsx",
      "specification": "PURPOSE: root App Router layout wiring fonts + global styles. HOW IMPLEMENT: import Metadata type; import { Inter, Outfit } from 'next/font/google'; import './globals.css'. const inter = Inter({ subsets:['latin'], variable:'--font-inter' }); const outfit = Outfit({ subsets:['latin'], variable:'--font-outfit' }). export const metadata = { title:'Student Profile System', description:'Manage student profiles' }. export default RootLayout({ children }: { children: React.ReactNode }) returning <html lang='en' className={`${inter.variable} ${outfit.variable}`}><body className='font-sans'>{children}</body></html>. EDGE CASES: next/font fetches Inter/Outfit at build time — an offline build can fail here (accepted). HOW TO TEST: compile-time via build."
    },
    {
      "path": "src/app/globals.css",
      "specification": "PURPOSE: global stylesheet wiring Tailwind layers and base theme. HOW IMPLEMENT: @tailwind base; @tailwind components; @tailwind utilities. @layer base: html,body { min-height:100% }; body { min-height:100vh; background-color:#020617; background-image:linear-gradient(160deg,#020617 0%,#0b1220 55%,#111a33 100%); background-attachment:fixed; color:#f1f5f9; -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale }. A universal focus fallback *:focus-visible { outline:none; box-shadow:0 0 0 2px #020617, 0 0 0 4px #10b981 } (inner gap in page bg, outer emerald ring following border radius). input,select,textarea,button { font-family:inherit; font-size:1rem; line-height:1.5rem } (>=16px so iOS never zooms on focus). @layer components: .card { @apply rounded-2xl border border-white/10 bg-white/5 shadow-lg backdrop-blur } (the frosted-glass surface). EDGE CASES: the universal fallback guarantees a visible focus indicator even where a component forgot explicit ring utilities; components layer their own focus-visible ring utilities on top. HOW TO TEST: compile-time via build; focus-ring behavior is exercised indirectly by the a11y suites."
    },
    {
      "path": "src/app/page.tsx",
      "specification": "PURPOSE: dashboard route as a React Server Component loading data via Prisma. SIGNATURES: export const dynamic='force-dynamic'; export default async function Page(). HOW IMPLEMENT: import { prisma } from '@/lib/prisma' and StudentDashboard. rows = await prisma.student.findMany({ orderBy:{ createdAt:'desc' } }). Map each row to a plain, fully serializable object matching the client Student type — copy id/studentId/fullName/email/major/gpa straight through and convert createdAt/updatedAt with .toISOString() (Prisma Date -> ISO string at the boundary). Render <main className='mx-auto w-full max-w-6xl px-4 py-10 sm:px-6 lg:px-8'> with a header (h1 'Student Profile System' font-display, and a subtitle paragraph) and <StudentDashboard initialStudents={students} />. EDGE CASES: force-dynamic opts out of caching so every request (and each router.refresh() after a mutation) reflects live DB state; the empty-database case is NOT special-cased here — the dashboard renders its own empty state; findMany is intentionally unbounded (the full dataset is needed client-side). HOW TO TEST: compile-time via build; runtime behavior covered by the component suites + manual run."
    },
    {
      "path": "src/app/api/students/route.ts",
      "specification": "PURPOSE: collection endpoints GET/POST /api/students. SIGNATURES: export const dynamic='force-dynamic'; export async function GET():Promise<NextResponse>; export async function POST(request:Request):Promise<NextResponse>. HOW IMPLEMENT: import NextResponse, { prisma }, and { parseGpa, validateStudent, type StudentInput } from '@/lib/validation'. Helper readString(source,key) returns the string value or '' when missing/non-string. GET: return NextResponse.json(await prisma.student.findMany({ orderBy:{ createdAt:'desc' } }), { status:200 }). POST: try parsed=await request.json() catch -> NextResponse.json({error:'Invalid JSON'},{status:400}); coerce parsed to a plain-object Record only when typeof==='object' && !==null && !Array.isArray, else {}; build input = { studentId/fullName/email/major via readString; gpa = (typeof rawGpa==='string'||'number') ? rawGpa : '' }; errors=validateStudent(input); if Object.keys(errors).length>0 -> {errors} 400; else try prisma.student.create({ data:{ studentId/fullName/email/major trimmed, gpa: parseGpa(input.gpa) as number } }) -> 201 with the created student; catch: if error has code 'P2002' -> 409 {errors:{studentId:'A student with this Student ID already exists'}}; else 500 {error:'Internal server error'}. ERROR/EDGE: malformed JSON -> 400; non-object JSON -> treated as {} so validators emit required messages -> 400; validation guarantees parseGpa returns a finite number before create. No auth/pagination (by design). HOW TO TEST: see route.test.ts."
    },
    {
      "path": "src/app/api/students/[id]/route.ts",
      "specification": "PURPOSE: item endpoints PATCH/DELETE /api/students/[id] addressed by numeric primary key. SIGNATURES: export const dynamic='force-dynamic'; export async function PATCH(request:Request,{params}:{params:{id:string}}):Promise<NextResponse>; export async function DELETE(request:Request,{params}:{params:{id:string}}):Promise<Response>. HELPERS: NUMERIC_ID_PATTERN=/^\\d+$/; MAX_ID_DIGITS=15; notFound() -> NextResponse.json({error:'Not found'},{status:404}); parseId(raw): if !NUMERIC_ID_PATTERN.test(raw) -> null; if raw.length>MAX_ID_DIGITS -> null; id=Number(raw); if !Number.isSafeInteger(id)||id<=0 -> null; else id. hasErrorCode(error,code): typeof object && !==null && 'code' in error && error.code===code. readString like the collection route. PATCH: id=parseId(params.id); if null -> notFound(); parse JSON (catch -> 400 {error:'Invalid JSON'}); coerce to plain-object Record else {}; build input (readString text fields; gpa passthrough when string|number else ''); errors=validateStudent(input); if any -> 400 {errors}; try prisma.student.update({ where:{id}, data:{ trimmed text, gpa: parseGpa(input.gpa) as number } }) -> 200; catch P2025 -> notFound(); P2002 -> 409 {errors:{studentId:'A student with this Student ID already exists'}}; else 500. DELETE: id=parseId; if null -> notFound(); try prisma.student.delete({ where:{id} }) -> new Response(null,{status:204}); catch P2025 -> notFound(); else 500 {error:'Internal server error'}. ERROR/EDGE: the strict /^\\d+$/ + safe-integer + id>0 guard means 'abc','0','-1','0x1','1e1','1.0' ALL resolve to 404 WITHOUT touching the DB (closing Number() coercion holes and overlong-digit Infinity/imprecision); a missing record (P2025) is a non-enumerating 404. HOW TO TEST: see route.test.ts."
    },
    {
      "path": "src/app/api/students/route.test.ts",
      "specification": "PURPOSE: node Vitest suite locking the full status contract for both routes WITHOUT a real DB. HOW IMPLEMENT: vi.mock('@/lib/prisma', () => ({ prisma:{ student:{ findMany:vi.fn(), create:vi.fn(), update:vi.fn(), delete:vi.fn() } } })) BEFORE importing the handlers; then import { prisma }, { GET, POST } from '@/app/api/students/route', { PATCH, DELETE } from '@/app/api/students/[id]/route'. Narrow each mock method for typing. DUPLICATE_MESSAGE='A student with this Student ID already exists'. VALID={ studentId:'S1', fullName:'Ada', email:'a@b.co', major:'CS', gpa:'3.5' }. makeRequest(method,body): new Request('http://localhost/api/students',{ method, headers:{'content-type':'application/json'}, body: typeof body==='string'?body:JSON.stringify(body) }) (string bodies sent verbatim to hit the malformed-JSON branch). params(id)->{params:{id}}. beforeEach vi.clearAllMocks(). HOW TO TEST — lock: GET 200 returns the findMany rows. POST 201 with created body and create called with {data:{studentId:'S1',fullName:'Ada',email:'a@b.co',major:'CS',gpa:3.5}} (trimmed text, numeric gpa). POST 400 for {...VALID,studentId:'',email:'nope'} with errors.studentId & errors.email and create NOT called. POST 400 {error:'Invalid JSON'} for '{not json' (create not called). POST 409 on create rejecting {code:'P2002'} with errors.studentId===DUPLICATE_MESSAGE. POST 500 on {code:'PXXXX'} and on a generic Error. PATCH 200 for a valid update (update called once). PATCH 404 for each id in ['abc','0','-1','0x1','1e1','1.0'] with update NOT called. PATCH 400 for a valid id but email 'nope' (update not called). PATCH 404 {error:'Not found'} on P2025. PATCH 409 on P2002. DELETE 204 with empty text body (delete called once). DELETE 404 on P2025. DELETE 404 for ['abc','0x1'] with delete not called. Intentionally assert NO 401/403 (out of scope by design)."
    },
    {
      "path": "src/components/StudentDashboard.tsx",
      "specification": "PURPOSE: 'use client' top-level orchestrator of metrics, search, the card grid, and the CRUD dialogs. SIGNATURES: default export StudentDashboard({ initialStudents }: { initialStudents: Student[] }); declare the local Student type (id:number; studentId/fullName/email/major:string; gpa:number; createdAt/updatedAt:string). HOW IMPLEMENT: import useMemo/useState, useRouter from 'next/navigation', type { FieldErrors, StudentInput } from '@/lib/validation', and SummaryCards/SearchFilter/StudentCard/StudentForm/DeleteDialog. State: query:''; formOpen:false; editing:Student|null; deleting:Student|null; submitting:false; serverErrors:FieldErrors={}; actionError:string|null. filtered=useMemo: q=query.trim().toLowerCase(); if q.length===0 return initialStudents; else filter where fullName/major/studentId .toLowerCase().includes(q) (ASCII-pragmatic). handleAdd (editing=null, clear serverErrors/actionError, open form); handleEdit(student) (editing=student, clear, open); handleDeleteRequest(student) (clear actionError, deleting=student); closeForm (formOpen=false, editing=null, serverErrors={}); closeDelete (deleting=null). handleSubmit(values:StudentInput): target=editing; url = target?`/api/students/${target.id}`:'/api/students'; method target?'PATCH':'POST'; setSubmitting(true); fetch JSON body=JSON.stringify(values); if response.ok -> closeForm()+router.refresh(); else if status 400||409 -> setServerErrors from body.errors (guarded); else setActionError('Something went wrong while saving. Please try again.'); catch -> setActionError('Unable to reach the server. Please try again.'); finally setSubmitting(false). handleConfirmDelete: target=deleting; if none return; fetch DELETE; ok -> closeDelete()+refresh; else banner 'Unable to delete this student. Please try again.'; catch network banner; finally clear submitting. RENDER: <div className='space-y-8'> — <SummaryCards students={initialStudents} /> (ALWAYS the full dataset, never filtered); <SearchFilter value={query} onChange={setQuery} onAdd={handleAdd} />; an actionError role='alert' banner when set; then conditional content — initialStudents.length===0 -> empty-state card ('No students yet' / prompt); else filtered.length===0 -> no-match card ('No matches' naming query.trim()); else a grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mapping filtered to a wrapper div key={student.id} className='animate-fade-in-up' style={{ animationDelay:`${index*60}ms` }} around <StudentCard student onEdit={handleEdit} onDelete={handleDeleteRequest} />. Mount <StudentForm> only when formOpen (props open, initial={editing}, serverErrors, submitting, onSubmit={handleSubmit}, onClose={closeForm}); mount <DeleteDialog> only when deleting (open, student={deleting}, submitting, onConfirm={handleConfirmDelete}, onCancel={closeDelete}). EDGE CASES: dialogs mount only while open so their focus move/restore run cleanly; there is no client cache — router.refresh() re-runs the server component for fresh data. HOW TO TEST: compile-time via build; child a11y locked by the .dom suites."
    },
    {
      "path": "src/components/SummaryCards.tsx",
      "specification": "PURPOSE: pure presentational aggregate metrics over the FULL dataset (no hooks/state). SIGNATURES: default export SummaryCards({ students }: { students: Student[] }); helpers computeAverageGpa(students:Student[]):string and computeTopMajor(students:Student[]):string; inner SummaryCard({ label, value, icon, gradient }: { label:string; value:string; icon:React.ReactNode; gradient:string }). Declare the local Student type. HOW IMPLEMENT: import Users, GraduationCap, TrendingUp from 'lucide-react'. computeAverageGpa: if length===0 return '—'; else sum gpa via reduce, average=Math.round((sum/total)*100)/100, return average.toFixed(2). computeTopMajor: count majors in a Map (insertion order preserves first-seen); iterate, keep the major with the strictly-greatest count so the first to reach a count wins ties; return '—' when empty. SummaryCard renders a gradient rounded-2xl card (bg-gradient-to-br ${gradient}, hover -translate-y-1) with the label, the value in font-display text-3xl truncate, and the icon in a rounded badge. SummaryCards renders <section aria-label='Summary metrics' className='grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3'> with three SummaryCards: 'Total Students'=String(length) (Users, brand gradient), 'Average GPA'=computeAverageGpa (GraduationCap, emerald gradient), 'Top Major'=computeTopMajor (TrendingUp, amber gradient). EDGE CASES: empty list yields '0'/'—'/'—' with no division-by-zero or NaN. HOW TO TEST: compile-time via build; aggregates exercised by seed data."
    },
    {
      "path": "src/components/SearchFilter.tsx",
      "specification": "PURPOSE: 'use client' controlled header row combining the search box with the Add action; holds no local state. SIGNATURES: default export SearchFilter({ value, onChange, onAdd }: { value:string; onChange:(v:string)=>void; onAdd:()=>void }). HOW IMPLEMENT: import Plus, Search from 'lucide-react'; SEARCH_INPUT_ID='student-search'. Outer flex-col gap-3 sm:flex-row sm:items-center. Search area (relative flex-1): a visually-hidden <label htmlFor={SEARCH_INPUT_ID} className='sr-only'>Search students by name, major, or student ID</label>; a decorative <Search aria-hidden> icon absolutely positioned left; an <input id={SEARCH_INPUT_ID} type='search' value={value} onChange={e=>onChange(e.target.value)} placeholder='Search by name, major, or ID…'> styled min-h-[48px] w-full rounded-2xl with left padding for the icon and focus-visible ring utilities. Then a primary <button type='button' onClick={onAdd}> (min-h-[48px], emerald-to-brand gradient, Plus icon + 'Add Student', focus-visible ring). EDGE CASES: the label is bound via htmlFor for AT; the input inherits font-size>=1rem from globals so iOS never zooms. HOW TO TEST: compile-time via build; used by the dashboard."
    },
    {
      "path": "src/components/StudentCard.tsx",
      "specification": "PURPOSE: 'use client' single-student frosted-glass card with edit/delete actions. SIGNATURES: default export StudentCard({ student, onEdit, onDelete }: { student:Student; onEdit:(s:Student)=>void; onDelete:(s:Student)=>void }); helper gpaTheme(gpa:number):{ dot:string; pill:string }. CONSTANTS: GPA_EMERALD_THRESHOLD=3.5, GPA_AMBER_THRESHOLD=2.5. HOW IMPLEMENT: import Pencil, Trash2 from 'lucide-react'. gpaTheme returns emerald classes when gpa>=3.5, amber when gpa>=2.5, else rose (a dot bg plus a matching translucent pill border/bg/text). Render <article className='card ...'> (hover scale/shadow) with a header row: left the fullName (font-display, truncate) and studentId as a muted badge; right two icon-only buttons each h-12 w-12 (>=48px), focus-visible ring, and a descriptive aria-label (`Edit ${student.fullName}` / `Delete ${student.fullName}`) calling onEdit(student)/onDelete(student) with aria-hidden icons. Below: the email in a truncating <p title={student.email}>. Footer (mt-auto): a truncating major chip and a GPA pill using gpaTheme with a colored dot and `GPA ${student.gpa.toFixed(2)}` in font-display. EDGE CASES: long email/major truncate to protect the layout; icon-only buttons carry aria-labels since the glyph conveys no text. HOW TO TEST: compile-time via build; thresholds exercised by seed data."
    },
    {
      "path": "src/components/StudentForm.tsx",
      "specification": "PURPOSE: 'use client' accessible add/edit modal backing both flows (add when initial===null, edit when initial is a Student). SIGNATURES: default export StudentForm({ open, initial, serverErrors, submitting, onSubmit, onClose }: { open:boolean; initial:Student|null; serverErrors:FieldErrors; submitting:boolean; onSubmit:(values:StudentInput)=>void; onClose:()=>void }). Import useEffect/useRef/useState; from '@/lib/validation' import COMMON_MAJORS, validateStudent, validateStudentId/validateFullName/validateEmail/validateMajor/validateGpa, and types FieldErrors/FieldName/StudentInput; declare the local Student type. HELPERS/CONSTANTS: type FormValues=Record<FieldName,string>; FIELD_ORDER=['studentId','fullName','email','major','gpa']; VALIDATORS = a Record<FieldName, (v:string)=>string|null> mapping each field to its single-value validator; ID_PREFIX='student-form'; DATALIST_ID=`${ID_PREFIX}-majors`; TITLE_ID=`${ID_PREFIX}-title`; FOCUSABLE_SELECTOR = the joined set 'a[href], button:not([disabled]), input:not([disabled]), select, textarea, [tabindex]:not([tabindex=\"-1\"])'; blankValues() -> all fields ''; valuesFromStudent(s) -> field values from s with gpa=String(s.gpa). STATE: values:FormValues (init from initial ? valuesFromStudent : blankValues); touched:Set<FieldName>=new Set(); errors:FieldErrors={}. REFS: panelRef (dialog panel/trap scope); inputRefs (Partial<Record<FieldName,HTMLInputElement|null>>). EFFECTS (all declared BEFORE the `if (!open) return null` guard): (a) empty-deps effect captures document.activeElement as the trigger and, on unmount, calls trigger?.focus?.() to restore focus; (b) effect on [open,initial] resets values (from initial or blank), touched=new Set(), errors={}; (c) effect on [serverErrors] merges any incoming server field errors onto errors and marks those fields touched; (d) effect on [open] focuses inputRefs.current.studentId. HANDLERS: handleChange(field,value) sets the value and deletes that field's error if present (clear-on-input); handleBlur(field) marks it touched and runs VALIDATORS[field](values[field]), setting or clearing that field's error; handleSubmit(event) preventDefault, allErrors=validateStudent(values), if any -> setErrors(allErrors), setTouched(new Set(FIELD_ORDER)), focus the first FIELD_ORDER field that has an error, and DO NOT call onSubmit; else onSubmit({ ...values }) (gpa stays a string, matching StudentInput). handleKeyDown: Escape -> preventDefault + onClose; Tab -> compute focusable via panel.querySelectorAll(FOCUSABLE_SELECTOR) and wrap at both ends (Shift+Tab from first -> last; Tab from last -> first; also wrap when activeElement is outside the panel). handleOverlayClick closes only when event.target===event.currentTarget. RENDER: a fixed inset-0 overlay (bg-slate-950/70 backdrop-blur, onClick={handleOverlayClick}) containing a panel <div ref={panelRef} role='dialog' aria-modal='true' aria-labelledby={TITLE_ID} onKeyDown={handleKeyDown} className='card ...'> with an <h2 id={TITLE_ID}> reading 'Edit student' when initial else 'Add student'. A <form noValidate onSubmit={handleSubmit}> maps FIELD_ORDER: each field id `${ID_PREFIX}-${field}`, a visible <label htmlFor> ('Student ID','Full Name','Email','Major','GPA'), and an <input> bound to values[field] with onChange/onBlur, ref into inputRefs, aria-invalid='true'+aria-describedby=errorId only when that field has an error, type='number' for gpa (inputMode='decimal', step='0.1', min='0', max='4') / 'email' for email / 'text' otherwise, and list={DATALIST_ID} for major (render a <datalist id={DATALIST_ID}> of COMMON_MAJORS <option> values). When a field has an error render <p id={errorId} className='... text-rose-300'>{error}</p> below it. Footer: a Cancel <button type='button' onClick={onClose}> and a submit <button type='submit' disabled={submitting}> whose label is 'Save changes' when initial else 'Add student'; both min-h-[48px] with focus rings. EDGE CASES: noValidate makes the aria messages the single source of truth (the number input's step/min/max are advisory only); GPA is submitted as a JSON string so the shared validators enforce the plain-decimal/range rules on both surfaces. HOW TO TEST: see StudentForm.dom.test.tsx."
    },
    {
      "path": "src/components/DeleteDialog.tsx",
      "specification": "PURPOSE: 'use client' confirmation modal (ARIA alertdialog) guarding destructive deletion. SIGNATURES: default export DeleteDialog({ open, student, submitting, onConfirm, onCancel }: { open:boolean; student:Student|null; submitting:boolean; onConfirm:()=>void; onCancel:()=>void }). Import useEffect/useRef and AlertTriangle from 'lucide-react'; declare the local Student type. CONSTANTS: FOCUSABLE_SELECTOR identical to StudentForm's; TITLE_ID='delete-dialog-title'; BODY_ID='delete-dialog-body'. REFS: panelRef (trap scope), cancelRef (focused on open). EFFECT (empty deps, declared BEFORE the early return): capture document.activeElement as the trigger, then cancelRef.current?.focus() (Cancel is the safe default, NOT Delete); cleanup restores focus to the trigger only when trigger.isConnected and it has a focus function. GUARD: after the hooks, `if (!open || !student) return null`. HELPERS: getFocusable() -> Array.from(panelRef.current.querySelectorAll(FOCUSABLE_SELECTOR)) (or [] if no panel); handleKeyDown: Escape -> preventDefault+onCancel; Tab -> if focusable is empty preventDefault (keep focus inside) else wrap at both ends exactly like StudentForm; handleOverlayClick closes when event.target===event.currentTarget. RENDER: a fixed inset-0 overlay (bg-slate-950/70 backdrop-blur) with onMouseDown={handleOverlayClick} (mouse DOWN so a Testing Library user.click on the backdrop fires it). Inside, a panel <div ref={panelRef} role='alertdialog' aria-modal='true' aria-labelledby={TITLE_ID} aria-describedby={BODY_ID} onKeyDown={handleKeyDown} className='card ...'>: a rose warning badge with <AlertTriangle aria-hidden>; an <h2 id={TITLE_ID}> reading `Delete ${student.fullName}?`; a <p id={BODY_ID}> naming the student and `(${student.studentId})` and stating the action cannot be undone. Actions (flex-col-reverse sm:flex-row sm:justify-end): a Cancel <button ref={cancelRef} type='button' onClick={onCancel}> and a destructive Delete <button type='button' onClick={onConfirm} disabled={submitting}> whose text is 'Deleting…' when submitting else 'Delete'; both min-h-[48px] with focus rings (Cancel emerald, Delete rose). EDGE CASES: the disabled Delete is excluded from FOCUSABLE_SELECTOR so the trap never lands on it while submitting. HOW TO TEST: see DeleteDialog.dom.test.tsx."
    },
    {
      "path": "src/components/StudentForm.dom.test.tsx",
      "specification": "PURPOSE: jsdom Vitest suite locking StudentForm's hand-written a11y behaviors. HOW IMPLEMENT: import useState, render/screen/within/waitFor from '@testing-library/react', userEvent, StudentForm, and type StudentInput; declare FOCUSABLE_SELECTOR identical to the component's and a fully-valid VALID input { studentId:'S1001', fullName:'Ada Lovelace', email:'a@b.co', major:'Computer Science', gpa:'3.9' }. Provide a Harness that owns open state and a real trigger <button>Open form</button> so clicking it leaves the trigger focused (the focus-restore target) and mirrors the dashboard; on close it flips open=false and calls the onClose spy. An openForm() helper renders the harness, clicks the trigger, awaits findByRole('dialog'), and returns { user, onSubmit, onClose, trigger, dialog }. A focusables(dialog) helper returns the ordered querySelectorAll matches. HOW TO TEST — lock: (1) on open the first field (getByLabelText /student id/i) has focus (waitFor). (2) typing 'nope' into Email then tabbing away shows 'Enter a valid email address', sets aria-invalid='true', and wires aria-describedby to an element containing that message. (3) after (2), typing any character into Email clears the message and removes aria-invalid. (4) focusing the last focusable control and pressing Tab wraps to the first. (5) focusing the first and pressing Shift+Tab wraps to the last. (6) pressing Escape calls onClose once. (7) after Escape, focus returns to the trigger (waitFor). (8) clicking 'Add student' on an empty form does NOT call onSubmit and shows all five required messages ('Student ID is required','Full name is required','Email is required','Major is required','GPA is required'); then filling every field with VALID and clicking again calls onSubmit exactly once with VALID."
    },
    {
      "path": "src/components/DeleteDialog.dom.test.tsx",
      "specification": "PURPOSE: jsdom Vitest suite locking DeleteDialog's a11y behaviors. HOW IMPLEMENT: import useState, render/screen from '@testing-library/react', userEvent, and DeleteDialog; a SAMPLE_STUDENT constant; a renderDialog(overrides) helper that renders the dialog open with vi.fn() onConfirm/onCancel spies and returns them; a Harness with a real trigger button (data-testid='trigger') that opens the dialog and whose onCancel both records the spy and closes (unmounts) the dialog so focus-restore can be observed. HOW TO TEST — lock: focus moves to the Cancel button on open; pressing Escape calls onCancel once; clicking the overlay backdrop (the alertdialog panel's parentElement) calls onCancel once (relies on the onMouseDown handler); clicking Delete calls onConfirm exactly once; the focus trap wraps at both ends (Tab from the Delete button wraps to Cancel; Shift+Tab from Cancel wraps to Delete); after opening from the real trigger and pressing Escape, onCancel fires once and focus returns to the trigger; when rendered with submitting=true the Delete button (name /deleting/i) is disabled."
    },
    {
      "path": "src/test/setup.ts",
      "specification": "PURPOSE: shared Vitest setup registered via setupFiles. HOW IMPLEMENT: a single import of '@testing-library/jest-dom' to augment expect with the jest-dom matchers (toHaveFocus, toBeDisabled, toHaveAttribute, aria-invalid checks) used by the jsdom component suites. EDGE CASES: harmless for the node suites (validation, route handlers), which never invoke DOM matchers. HOW TO TEST: the *.dom.test.tsx suites use these matchers without importing them per file."
    },
    {
      "path": "README.md",
      "specification": "PURPOSE: concise developer documentation (not a spec; do not restate design internals). HOW IMPLEMENT: a Markdown file with — a one-paragraph summary (Next.js 14 App Router + React 18 in TypeScript, TailwindCSS v3, Prisma over local SQLite; view/add/edit/delete students with client-side search and summary metrics; validation lives in one shared module so client and server can never drift). A Prerequisites section (Node.js 18+, npm). A Setup section listing the ordered commands `npm install`, `npx prisma generate`, `npx prisma db push` (creates prisma/dev.db), `npm run db:seed` (optional), `npm run dev`, noting the app serves at http://localhost:3000 and that the seed produces a non-empty first view. A Commands table for dev/build/start/lint/test/db:push/db:seed. A 'No .env required' note (the SQLite url is a hardcoded literal; no env file is created, read, or parsed). A Testing section explaining `npm run test` runs three Vitest surfaces (validation logic in node; API route handlers against a mocked Prisma client in node; component a11y behavior in jsdom) with remaining styling verified by build + lint. A Features section: the four CRUD flows, client-side search, and summary metrics — stating the deliberate scope difference that summary metrics ALWAYS reflect the full dataset while the search box filters only the visible cards. IMPORTANT — describe the security/deployment model HONESTLY and match the actual code: state plainly this is a LOCAL, SINGLE-USER tool with NO authentication, authorization, or pagination by design, and that access control and request-size limits are a deployment/reverse-proxy concern. Do NOT document CSRF/DNS-rebinding guards, 403/413/415 responses, loopback-only binding, body-size caps, or control-character/bidi filtering — none of these exist in the code, and claiming them would be false. HOW TO TEST: the documented commands match package.json scripts exactly."
    }
  ]
}
```
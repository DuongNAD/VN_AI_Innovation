/**
 * Minimal Next.js configuration.
 *
 * App Router is the default in Next.js 14, and this project needs no
 * experimental flags, so an empty configuration object is sufficient.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;